# Release History

## 2026-08-10 00:52 JST
- v2.67
	- 左画面・実機で `予定の入力・修正` が押せない事象に対し、ホームの予定入力ボタンを常時有効化。
	- 予定入力ボタンのクリックイベントを条件分岐外へ移動し、表示モードに関係なく必ず遷移できるよう修正。
	- 遷移先日付は `表示中の日付` を優先し、未取得時は `homeDisplayDateKey` / 当日へフォールバックするよう補強。
	- `index.html` の表示バージョンを `2.66` から `2.67` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.67` に更新。

## 2026-08-10 00:47 JST
- v2.66
	- 8/10表示で予定入力できない原因だった `前日表示時に予定入力ボタンを非表示` する条件を修正。
	- `予定の入力・修正` ボタンを表示モードに関係なく描画し、許可時は押下できるよう変更。
	- 予定入力へ遷移する際の対象日を `表示中の日付` に合わせるよう調整し、左で表示した日付にも正しく入力できるよう修正。
	- `index.html` の表示バージョンを `2.65` から `2.66` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.66` に更新。

## 2026-08-10 00:44 JST
- v2.65
	- 左移動で表示した過去日で `予定の入力・修正` が開けない問題を是正。
	- `isPreviousView / isPastView` 判定時の `canOpenPlanning` を常に有効化し、左側遷移先でも予定入力できるよう変更。
	- 8/10限定の一時例外条件を廃止し、日付依存しない挙動へ整理。
	- `index.html` の表示バージョンを `2.64` から `2.65` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.65` に更新。

## 2026-08-10 00:41 JST
- v2.64
	- 仕様分離: 当日タスクフローが完了扱いでも、`宿題・課題` は当日から開けるように変更。
	- 個別対応: 過去日表示でも `2026-08-10` のみ `予定の入力・修正` を開ける一時例外を追加。
	- これにより、左移動で 8/10 を表示したケースでも予定入力へ進めるよう是正。
	- `index.html` の表示バージョンを `2.63` から `2.64` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.64` に更新。

## 2026-08-10 00:27 JST
- v2.63
	- 前日の終了処理を完了した直後に、当日（例: 8/10）が `dayClosed` 扱いになって予定入力できない不具合を修正。
	- 前日終了処理完了時の `dayClosed` を `true` から `false` に変更し、当日フローを閉じないよう是正。
	- これにより、当日が未完了であればホームの `予定の入力・修正` を開けるよう改善。
	- `index.html` の表示バージョンを `2.62` から `2.63` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.63` に更新。

## 2026-08-10 00:25 JST
- v2.62
	- 予定入力の案内文 `新規のタスクはここ` の位置を再調整し、`例: 原田先生` の表示位置から右へ約3cmの位置に変更。
	- 案内文は新規入力時のみ表示し、フォーカス時・入力開始後は非表示になる挙動を維持。
	- `index.html` の表示バージョンを `2.61` から `2.62` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.62` に更新。

## 2026-08-10 00:23 JST
- v2.61
	- 予定入力カード内の案内文 `新規のタスクはここ` を、入力欄横から同カード中央へ移動。
	- 案内文の配置を入力欄への絶対配置から中央寄せ表示へ変更し、カード内で見つけやすく調整。
	- `index.html` の表示バージョンを `2.60` から `2.61` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.61` に更新。

## 2026-08-10 00:14 JST
- v2.60
	- №158-10 日付切替時の `planTimes` 引き継ぎ判定を調整し、`外出なし`（`none`）を未入力扱いにせず保持するよう修正。
	- 引き継ぎルールを明確化：時刻入力ありは保持、`外出なし` は保持、空値などの未入力のみ既存初期値へ補完。
	- `index.html` の表示バージョンを `2.59` から `2.60` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.60` に更新。

## 2026-08-10 00:10 JST
- v2.59
	- №158 日付切替時（`parsed.dateKey !== todayKey`）の state 再生成で、翌日用として保存済みの `planTimes` を引き継ぐよう修正。
	- 前日に翌日予定として入力・保存した `起床 / 出発 / 帰宅 / 勉強開始` が、翌日になっても初期値へ戻らず保持されるよう改善。
	- 翌日文脈（`planningTargetDateKey` または `confirmedPlan.targetDateKey` が当日）を満たさない場合は、従来どおり `createDefaultPlanTimes()` を使用。
	- `dayClosed` / `goPressedAt` / `planFor` の日付切替仕様は変更なし。
	- `index.html` の表示バージョンを `2.58` から `2.59` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.59` に更新。

## 2026-08-09 23:58 JST
- v2.58
	- タスク名入力欄のプレースホルダー `例: 原田先生` を元の色へ戻すよう調整。
	- `新規のタスクはここ` を、入力欄内の `例: 原田先生` のすぐ右に表示する点滅案内へ変更。
	- 入力開始後とフォーカス時は、点滅案内を非表示にするよう調整。
	- `index.html` の表示バージョンを `2.57` から `2.58` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.58` に更新。

## 2026-08-09 23:57 JST
- v2.57
	- 予定入力画面の `次の1件を入力してください。` を赤太字で表示するよう変更。
	- タスク名入力欄（`例: 原田先生`）のプレースホルダーを赤字表示に変更。
	- 新規入力時のみ `新規のタスクはここ` を赤字で点滅表示する案内を追加。
	- `index.html` の表示バージョンを `2.56` から `2.57` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.57` に更新。

## 2026-08-09 23:53 JST
- v2.56
	- 予定コピー後ポップアップの確定ボタン文言を `わかりました` から `黒の手帳もラストチェックした` に変更。
	- `index.html` の表示バージョンを `2.55` から `2.56` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.56` に更新。

## 2026-08-09 23:51 JST
- v2.55
	- 予定コピー後ポップアップの文言を、より具体的な送信案内に変更。
	- 見出し `送信のお願い` を削除し、本文中心の表示へ調整。
	- `index.html` の表示バージョンを `2.54` から `2.55` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.55` に更新。

## 2026-08-09 23:48 JST
- v2.54
	- `予定を確定してコピー` 押下後の通知を、単純な alert から温かみのある専用ポップアップに変更。
	- ポップアップ文言を強調し、`親へメッセージを送信` する案内を見やすく表示。
	- コピーに成功した場合のみポップアップを表示するよう変更。
	- `index.html` の表示バージョンを `2.53` から `2.54` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.54` に更新。

## 2026-08-09 23:45 JST
- v2.53
	- 統合画面の下部ボタン文言を `ホームへ戻る` から `確定してホームへ戻る` に変更。
	- `予定を確定してコピー` 押下時に、`親へ送信すること` のポップアップを表示するよう変更。
	- コピー文面の内容・形式は変更なし。
	- `index.html` の表示バージョンを `2.52` から `2.53` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.53` に更新。

## 2026-08-09 23:39 JST
- v2.52
	- 統合画面のボタン文言を `予定をコピー` から `予定を確定してコピー` に変更。
	- コピー処理（親向け報告文の内容・形式）は変更なし。
	- `index.html` の表示バージョンを `2.51` から `2.52` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.52` に更新。

## 2026-08-09 23:36 JST
- v2.51
	- №157 予定確認画面と親への予定報告画面を統合し、予定入力後は統合画面へ直接遷移する導線へ変更。
	- 旧 `planConfirm` 画面を経由せず、既存の `confirmPlan`（`confirmedPlan` 保存・`goPressedAt` 記録）を実行したうえで統合画面を表示するよう変更。
	- 統合画面に、対象日・起床/出発/帰宅/勉強開始（2列×2行）・タスク名/予定時間/内容・学習予定時間の合計を表示。
	- 統合画面の操作を `戻って修正` / `予定をコピー` / `ホームへ戻る` に整理。
	- 親向けコピー文は既存の生成関数をそのまま使用し、内容・形式を変更していない。
	- `index.html` の表示バージョンを `2.50` から `2.51` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.51` に更新。

## 2026-08-09 23:22 JST
- v2.50
	- №157 対応として、予定入力画面のボタン文言を `最終確認へ` から `予定を確認する` に変更。
	- ボタンの機能・遷移先（最終確認画面へ）は変更なし。
	- `index.html` の表示バージョンを `2.49` から `2.50` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.50` に更新。

## 2026-08-09 05:xx JST
- v2.49
	- 2026-08-09 だけに効いていた当日表示の強制復帰処理を無効化。
	- ホーム画面で 8/10 を選んだ直後に 8/9 へ戻る現象を解消。
	- 父親端末では発生しなかった差分を、長男側の保存/再読込でも同じように扱えるよう修正。
	- `index.html` の表示バージョンを `2.48` から `2.49` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.49` に更新。

## 2026-08-09 04:51 JST
- v2.48
	- №156 `今日のタスク` 通常表示件数を修正し、未完了優先で合計最大4件へ変更。
	- 未完了を先頭から最大4件表示し、4件未満の場合のみ完了タスクで残り枠を補う表示へ変更。
	- 通常表示外タスクは従来どおり `すべて見る` で全件確認できる仕様を維持。
	- タイトル/説明文非表示、`未完了の確認` 配置、コンパクトカード、タスク押下動作、通知関連仕様は変更なし。
	- `index.html` の表示バージョンを `2.47` から `2.48` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.48` に更新。

## 2026-08-09 04:42 JST
- v2.47
	- №155 `今日のタスク` の通常表示件数を、未完了最大3件＋完了最大1件に変更。
	- 通常表示外のタスクがある場合のみ `すべて見る` を表示し、押下で全タスク表示できるよう調整。
	- 通常表示の下に `すべて見る`、その下に従前どおり `未完了の確認` を表示する配置を維持。
	- №154 のコンパクトなカード型表示、タスク押下時の既存動作、通知関連仕様は変更なし。
	- `index.html` の表示バージョンを `2.46` から `2.47` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.47` に更新。

## 2026-08-09 04:37 JST
- v2.46
	- 上部ナビの `ホーム` 右横に、同サイズの `今日のタスク` ボタンを追加。
	- `今日のタスク` ボタン押下で `今日のタスク` 一覧画面へ遷移する導線を追加。
	- `index.html` の表示バージョンを `2.45` から `2.46` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.46` に更新。

## 2026-08-09 04:30 JST
- v2.45
	- `今日のタスク` 画面のタイトルと説明文を削除。
	- タスク2行目のラベルを `明細` から `内容` に変更。
	- `内容：` の後ろに1マス空けて表示するよう調整。
	- `index.html` の表示バージョンを `2.44` から `2.45` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.45` に更新。

## 2026-08-09 04:26 JST
- v2.44
	- `今日のタスク` 一覧の並び順を調整し、未完了タスクが上位、完了タスクがその下に並ぶよう変更。
	- タスク完了時は一覧上で未完了群から完了群へ移る並び替えになるよう調整。
	- `index.html` の表示バージョンを `2.43` から `2.44` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.44` に更新。

## 2026-08-09 04:07 JST
- v2.43
	- №154 `今日のタスク` 一覧カードの内部レイアウトをコンパクト化。
	- カード型デザインは維持したまま、1行目を `状態 / タスク名 / 予定 / 実績`、明細ありのみ2行目 `明細：...` とする表示へ変更。
	- 明細が空のタスクでは `明細：－` を出さず、基本1行表示へ変更。
	- カード内部の上下余白・項目間余白を縮小し、上位5件をスマホで見やすい高さへ調整。
	- カード押下時の動作、`すべて見る`、№149〜№153 の仕様、通知関連仕様は変更なし。
	- `index.html` の表示バージョンを `2.42` から `2.43` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.43` に更新。

## 2026-08-09 04:07 JST
- v2.42
	- №153 `未完了の確認` を復元。
	- 従前の既存関数 `renderExecutionSubmissionChecklistSection()` と既存データ `submissionChecklist...` 系をそのまま使い、`今日のタスク` 一覧画面の下段にも再表示するよう復旧。
	- タスク本体完了後も未完了の確認項目が残る既存仕様を維持したまま、`確認を続ける` から個別確認項目を完了できるよう復旧。
	- №149〜№152 の中断/再開・ホーム切替・上位5件一覧仕様は変更なし。
	- 通知関連仕様は変更なし。
	- `index.html` の表示バージョンを `2.41` から `2.42` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.42` に更新。

## 2026-08-09 03:56 JST
- v2.41
	- №152 「タスク実行へ」押下時の遷移先を、個別実行画面ではなく `今日のタスク` 一覧画面へ変更。
	- `今日のタスク` 一覧画面に、状態・タスク名・予定時間・実績時間・明細を表示する上位5件表示を追加。
	- `すべて見る` で残りの今日タスクも一覧表示できるよう追加。
	- 一覧を開いただけでは、対応中タスクを中断せず、開始・再開・履歴追加も発生しないよう維持。
	- 一覧で `【対応中】` は個別実行画面を開き、`【再開】` と `【未開始】` は既存の自動中断付き開始/再開処理へ接続。
	- `【完了】` タスクは一覧表示のみとし、押しても再開・開始しないよう調整。
	- №149・№151 の中断/再開仕様、通知関連仕様は変更なし。
	- `index.html` の表示バージョンを `2.40` から `2.41` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.41` に更新。

## 2026-08-09 03:50 JST
- v2.40
	- 2026-08-09 の当日についてのみ、ホーム画面の当日完了扱いを一時解除する例外処理を追加。
	- `dayClosed = true` で閉じていた場合も、予定・タスク・宿題の当日導線を再度開けるよう調整。
	- 当日タスクが全件 `done` の場合でも、今回に限ってホーム画面の当日操作を再開できるよう調整。
	- 既存の予定・タスク・履歴データは削除しない。
	- `index.html` の表示バージョンを `2.39` から `2.40` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.40` に更新。

## 2026-08-09 03:36 JST
- v2.39
	- ホーム画面の `予定の入力・修正` ボタン配色を再調整し、今日の薬の `確認する` ボタンと同じ薄い青系に変更。
	- `index.html` の表示バージョンを `2.38` から `2.39` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.39` に更新。

## 2026-08-09 03:36 JST
- v2.38
	- ホーム画面の `予定の入力・修正` ボタンを、今日の薬の `確認する` カードと同系色の配色へ変更。
	- `index.html` の表示バージョンを `2.37` から `2.38` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.38` に更新。

## 2026-08-09 03:32 JST
- v2.37
	- ホーム画面の最下部に小さな `ログオフ` ボタンを追加。
	- 追加したホーム画面の `ログオフ` ボタンを既存のログオフ処理へ接続。
	- `index.html` の表示バージョンを `2.36` から `2.37` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.37` に更新。

## 2026-08-09 03:03 JST
- v2.36
	- №151 ホーム画面からのタスク切替に対応。
	- ホーム画面のタスク一覧で、現在 `【対応中】` 以外のタスクを押した場合に、押したタスクをそのまま実行対象へ切替するよう変更。
	- `【再開】` タスク押下時は、現在の対応中タスクを自動中断してから押下タスクを再開する導線に変更。
	- 未開始タスク押下時は、現在の対応中タスクを自動中断してから押下タスクを開始する導線に変更。
	- 現在 `【対応中】` のタスク押下時は、従来どおり当該タスクの実行画面を開く動作を維持。
	- `index.html` の表示バージョンを `2.35` から `2.36` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.36` に更新。

## 2026-08-09 02:58 JST
- v2.35
	- ホーム画面の実行中ステータス表示を記号表示から `【対応中】` へ変更。
	- `【対応中】` を青字で点滅表示するスタイルを追加。
	- 中断中ステータス `【再開】` の点滅挙動は維持。
	- `index.html` の表示バージョンを `2.34` から `2.35` へ更新（+0.01）。
	- `index.html` の `style.css` / `app.js` 読み込みクエリを `v=2.35` に更新。

## 2026-08-09 02:49 JST
- v2.34
	- 画面表示不具合（Unexpected token ')'）を修正。
	- `bindPlanningEvents` の折りたたみハンドラに混入していた誤コードを除去し、通常の予定入力折りたたみ動作へ復旧。
	- `index.html` の `app.js` 読み込みクエリを `v=2.34` に更新。
	- 表示バージョンを `2.33` から `2.34` へ更新（+0.01）。

## 2026-08-09 02:46 JST
- v2.33
	- №149 複数タスク中断・再開対応。
	- 中断状態を `task.status = paused` でタスク単位に保持し、複数中断を同時保持できるように修正。
	- 実行中は `state.running` で1件のみ保持する方式を維持し、別タスク開始/再開時は現在の実行中タスクを自動で中断してから切替。
	- 実行画面に中断中タスクの「再開」操作を追加し、個別に再開できるように修正。
	- 履歴は初回のみ「開始」、2回目以降（中断後・実績秒数あり）は「再開」として記録するように修正。
	- 「今日は終了」の未完了対象を `pending` に加えて `paused` も含むように修正。
	- 予定内容、定期入力、テンプレート、宿題・課題、持ち物、日付切替、Departure、Task Finish、Task Recheck、通知、音、バイブ、Exact Alarm の仕様は変更なし。
	- 表示バージョンを `2.32` から `2.33` へ更新（+0.01）。

## 2026-08-09 02:35 JST
- v2.32
	- 画面表示不具合の原因だった `app.js` の構文エラーと `createRecurringForm` 未定義を修正して起動を復旧。
	- 帰宅後報告の「コピーしてLINEを開く」ボタンとLINE起動処理を削除。
	- `index.html` の `app.js` 読み込みクエリを `v=2.32` に更新し、最新スクリプトを確実に読み込むよう調整。
	- 表示バージョンを `2.31` から `2.32` へ更新（+0.01）。

## 2026-08-09 02:31 JST
- v2.31
	- 画面が表示されない不具合対応として、`index.html` の `app.js` 読み込みクエリを `v=2.31` に更新し、古いモジュールキャッシュを回避。
	- 表示バージョンを `2.30` から `2.31` へ更新（+0.01）。

## 2026-08-09 02:25 JST
- v2.30
	- 帰宅後報告の「コピーしてLINEを開く」ボタンとコピー時のLINE起動処理を最終削除。
	- 送信完了の導線だけを残す形に確定。
	- 表示バージョンを `2.29` から `2.30` へ更新（+0.01）。

## 2026-08-09 02:24 JST
- v2.29
	- 帰宅後報告の「コピーしてLINEを開く」ボタンを最終的に削除。
	- コピー時のLINE起動処理も削除し、送信完了の導線だけ残すようにした。
	- 表示バージョンを `2.28` から `2.29` へ更新（+0.01）。

## 2026-08-09 02:23 JST
- v2.28
	- 帰宅後報告の「コピーしてLINEを開く」ボタンとコピー時のLINE起動処理を削除。
	- 送信完了の導線だけを残すように整理。
	- 表示バージョンを `2.27` から `2.28` へ更新（+0.01）。

## 2026-08-09 02:21 JST
- v2.27
	- 帰宅後報告の「コピーしてLINEを開く」ボタンを削除。
	- LINEを開くコピー処理を外し、送信完了の導線だけ残すように整理。
	- 表示バージョンを `2.26` から `2.27` へ更新（+0.01）。

## 2026-08-09 02:20 JST
- v2.26
	- 帰宅時チェックの「カレンダー、アプリに登録！！」を赤字太字で復元。
	- 帰宅時チェックの報告文も同じ文言に合わせて復元。
	- 表示バージョンを `2.25` から `2.26` へ更新（+0.01）。

## 2026-08-09 02:18 JST
- v2.25
	- 帰宅時チェックの「宿題・課題・プリントの有無」を通常表示に戻し、赤字太字を解除。
	- 帰宅時チェックの報告文も同じ短い表現に戻す。
	- 表示バージョンを `2.24` から `2.25` へ更新（+0.01）。

## 2026-08-09 02:17 JST
- v2.24
	- 帰宅時チェックの「宿題題・課題・プリントの有無」を「宿題題・課題・プリントの有無（カレンダー・アプリ登録！！）」に変更し、赤字太字で表示。
	- 帰宅時チェックの報告文も同じ文言に合わせて更新。
	- 表示バージョンを `2.23` から `2.24` へ更新（+0.01）。

## 2026-08-09 02:14 JST
- v2.23
	- 帰宅時チェックの「宿題の有無」を「宿題題・課題・プリントの有無（アプリ登録！！）」に変更。
	- 帰宅時チェックの「家庭教師・親への返信」を「家庭教師・親への返信（依頼されたことの回答）」に変更。
	- 表示バージョンを `2.22` から `2.23` へ更新（+0.01）。

## 2026-08-09 02:08 JST
- v2.22
	- 出発前チェックの4番を「今日の予定（グーグルカレンダー、黒手帳含む）に目を通したか」に変更。
	- 出発前チェックの5番を「スマホを玄関へ置くか、バッグ等に入れたか」に変更。
	- 表示バージョンを `2.21` から `2.22` へ更新（+0.01）。

## 2026-08-09 01:59 JST
- v2.21
	- 出発前チェックを出発の60分前からホーム上部に表示し、15分を切ったら全画面の出発前チェックへ切り替えるよう修正。
	- ホーム上部の出発前チェック表示は完了するまで維持し、60分より前は出さないよう調整。
	- 表示バージョンを `2.20` から `2.21` へ更新（+0.01）。

## 2026-08-09 01:46 JST
- v2.20
	- ホームの「次の日を見る」の確認アラート文言を「翌日（MM/DD）の予定の表示ですが、問題ないですか？」に変更。
	- 表示バージョンを `2.19` から `2.20` へ更新（+0.01）。

## 2026-08-09 01:46 JST
- v2.19
	- 当日中にホームの「次の日を見る」を押した場合に、翌日表示へ進む前の確認アラートを追加。
	- アラート文言を「当日中に次の日を見るを押すと、翌日（MM/DD）の予定を表示します。問題ないですか？」に変更。
	- 表示バージョンを `2.18` から `2.19` へ更新（+0.01）。

## 2026-08-09 01:44 JST
- v2.18
	- ホームの前日表示で「予定の入力・修正」を押せないようにし、タスク実行と同じく終了後の導線を無効化。
	- ホームの予定入力ボタン文言を「この日の予定を入力」から「予定の入力・修正」に変更。
	- 表示バージョンを `2.17` から `2.18` へ更新（+0.01）。

## 2026-08-09 01:40 JST
- v2.17
	- ホーム画面上部の表示日を削除し、日付移動を `前日を見る` / `次の日を見る` の左右ボタンへ変更。
	- `この日の予定を入力` をホームの表示日連動ボタンへ変更し、予定入力画面はホームから受けた日付をそのまま対象にするよう修正。
	- 予定入力画面の `今日/明日` 選択を削除し、ホームへ戻っても同じ表示日に戻るよう修正。
	- 定期入力、テンプレート、宿題・課題、持ち物の登録内容・処理、Departure、Task Finish、Task Recheck、通知、音、バイブ、Exact Alarm 関連は変更なし。
	- 表示バージョンを `2.16` から `2.17` へ更新（+0.01）。

## 2026-08-09 00:49 JST
- v2.16
	- 日付切替テストの `元に戻す` で、テスト前の前日表示ではなく当日表示へ戻るよう正規化を追加。
	- 復元時に `homeViewMode` / `dayClosed` / `goPressedAt` / `planFor` を当日基準へ戻し、8/9 へ戻したあとに 8/10 へ飛ばないよう修正。
	- 通常の日付切替処理、Departure、Task Finish、Task Recheck、通知、音、バイブ、Exact Alarm 関連は変更なし。
	- 表示バージョンを `2.15` から `2.16` へ更新（+0.01）。

## 2026-08-09 00:49 JST
- v2.15
	- 設定画面の開発用確認欄に `翌日切替テスト` と `元に戻す` を追加し、端末時刻を変えずに翌日状態を一時再現できるよう対応。
	- テスト開始前の状態はスナップショットとして保存し、復元時に元の状態へ戻せるよう対応。
	- 通常の日付切替処理、Departure、Task Finish、Task Recheck、通知、音、バイブ、Exact Alarm 関連は変更なし。
	- 表示バージョンを `2.14` から `2.15` へ更新（+0.01）。

## 2026-08-09 00:49 JST
- v2.14
	- 日付切替時に `dayClosed` / `goPressedAt` / `planFor` を翌日に持ち越さないよう初期化を追加。
	- 日付切替後の予定入力は当日基準で初期化し、起動直後の初期対象日が翌日に進みすぎないよう修正。
	- 前日未完了判定は前日の保存済み状態のみを見るまま維持し、`dayClosed=true` の前日は未完了扱いにしないよう調整。
	- 未完了の宿題・課題の翌日引き継ぎは既存仕様のまま維持。
	- 表示バージョンを `2.13` から `2.14` へ更新（+0.01）。

## 2026-08-05 02:06 JST
- v2.13
	- v2.12で追加した Departure通知・Task Finish通知のAndroidネイティブ直接バイブ処理を削除し、TimedNotificationPublisher を v2.11 相当へ差し戻し。
	- Departure と Task Finish の音、NotificationChannel、AudioAttributes、Exact Alarm、通知予定時刻、channelId は変更なし。
	- Task Recheck は変更なし。
	- 予定入力画面の「時間」「持ち物・やること」を初期状態で閉じる変更は維持。
	- 表示バージョンを `2.12` から `2.13` へ更新（+0.01）。

## 2026-08-05 01:54 JST
- v2.12
	- Departure通知とTask Finish通知について、通知発火時にAndroidネイティブ側（TimedNotificationPublisher）で直接バイブを実行する処理を追加。
	- `extra.source` が `departure-reminder` または `task-finish` のときのみ、同一バイブパターン `0,500,300,500` を実行するよう追加。
	- Task Recheck は対象外として変更なし。
	- 予定入力画面を開くたびに「時間」「持ち物・やること」の開閉状態を初期化し、初期表示を閉じるよう変更。
	- 登録済みタスク以下の表示、入力内容、保存処理、表示項目は変更なし。
	- 音源、NotificationChannel、AudioAttributes、Exact Alarm、通知予定時刻、channelIdは変更なし。
	- 表示バージョンを `2.11` から `2.12` へ更新（+0.01）。

## 2026-08-05 01:18 JST
- v2.11
	- Task Finish通知チャネルのみ、既存端末設定の残留を避けるため channelId プレフィックスを `task-finish-alarm-v3-` へ更新。
	- Task Finish通知チャネル作成時に `vibration: true` を維持したまま、明示的な `vibrationPattern` を追加。
	- Departure と Task Recheck の通知コード・設定・通知経路は変更なし。
	- Task Finish の音源、Exact Alarm、AudioAttributes、通知時刻処理は変更なし。
	- 表示バージョンを `2.10` から `2.11` へ更新（+0.01）。

## 2026-08-05 00:47 JST
- v2.10
	- AndroidManifest.xml に `SCHEDULE_EXACT_ALARM` 権限を追加。
	- 設定画面の通知設定内に、正確なアラームの許可状態表示と設定導線を追加。
	- 正確なアラーム未許可時は、Task Finish開始時に1回だけ設定案内ダイアログを表示するよう追加。
	- Task Finish の `taskFinishNotifyAtMs` と pending確認ロジックは維持。
	- Departure と Task Recheck の通知コード・設定・音源・通知経路は変更なし。
	- 表示バージョンを `2.09` から `2.10` へ更新（+0.01）。

## 2026-08-05 00:14 JST
- v2.09
	- Task Finish通知だけ、開始時・再開時に絶対通知予定日時 `taskFinishNotifyAtMs` を計算して実行中状態へ保存するよう変更。
	- Task Finish通知予約時は、保存済み `taskFinishNotifyAtMs` をそのまま使用し、途中で再計算しないよう変更。
	- Task Finish通知予約直後に `LocalNotifications.getPending()` で同一notificationIdの存在確認を行い、見つからない場合は1回だけ再予約するよう変更。
	- 再予約後もpendingに存在しない場合は、タイマーや時間到達画面を止めずに `Task Finish通知の予約に失敗しました` を表示するよう変更。
	- Departure通知、Task Recheck通知、NotificationChannel、AudioAttributes、選択済み音源URIの扱いは変更なし。
	- 表示バージョンを `2.08` から `2.09` へ更新（+0.01）。

## 2026-08-04 23:52 JST
- v2.08
	- Task Finish 通知予約処理を Departure と同型の `await` ベース処理へ統一し、予約関数を非同期戻り値ありに変更。
	- Task Finish の開始時・復帰時・復元時の呼び出しを `void scheduleTaskFinishNotificationForRunningTask(...)` へ統一。
	- Task Finish 通知の投稿は既存の `scheduleLocalNotification()` 経路を継続使用。
	- Departure 通知処理、Task Recheck 通知処理、通知設定画面、音源URI保存方式は変更なし。
	- 表示バージョンを `2.07` から `2.08` へ更新（+0.01）。

## 2026-08-04 23:08 JST
- v2.07
	- 出発前通知は変更せず、タスク予定時間通知のみ修正。
	- タスク通知のチャネル解決で `audioUsage: alarm` を強制し、出発前通知と同じアラーム経路に揃えるよう調整。
	- タスク通知の固定プリセット `バイシクル` 選択時は、システム探索URIではなくバンドル済み raw 音源 `bicycle` を使用するよう変更。
	- 起動時の固定プリセット再解決でも、タスク通知の `バイシクル` は raw `bicycle` を維持するよう調整。
	- 20分後再確認通知の経路は今回変更なし。
	- 表示バージョンを `2.06` から `2.07` へ更新（+0.01）。

## 2026-08-04 17:36 JST
- v2.06
	- タスク予定時間到達時の独自ピピピ再生経路（`triggerAlertFeedback`）をタスク通知フローから切り離し、通知音をNotificationChannel側に一本化。
	- タスク予定時間到達時にローカル通知を事前キャンセルしていた処理を除去し、タスク通知がチャネル設定音で鳴るよう調整。
	- 既存端末に残る旧タスク通知チャネル影響を避けるため、タスク通知系チャネルIDプレフィックスを `task-finish-alarm-v2-` / `task-recheck-sound-v2-` へ更新（出発前通知は変更なし）。
	- 表示バージョンを `2.05` から `2.06` へ更新（+0.01）。

## 2026-08-04 15:07 JST
- v2.05
	- 宿題・課題ブロックを、案件ごと・日付ごとの実施履歴表示へ変更。
	- 未完了案件は `homeworkTasks` と個別項目の完了日時を保持したまま翌日へ引き継ぎ、完了済み案件は完了日時・個別項目日時の履歴スナップショットを保存するよう変更。
	- 完了済み宿題・課題は全体完了日を含めて最大7日間だけ完了履歴へ表示し、それ以降は非表示にするよう変更。
	- 課題登録時刻と提出区分は表示しないよう変更し、履歴コピー文面も同じ構成へ更新。
	- 表示バージョンを `2.04` から `2.05` へ更新（+0.01）。

## 2026-08-04 14:24 JST
- v2.04
	- 完了履歴の通常タスク詳細で、`開始 14:15` の行頭を宿題・課題の `次：かばんへ入れた` と同じ字下げ方式に統一。
	- アクション行を `padding-left` ではなく、宿題・課題と同じ空白文字ベースのインデントへ揃えた。
	- 表示バージョンを `2.03` から `2.04` へ更新（+0.01）。

## 2026-08-04 14:23 JST
- v2.03
	- 完了履歴の通常タスク詳細で、`開始 14:15` の行頭位置を案件名の先頭と揃えるため、アクション行専用の左インデントを追加。
	- 表示側のみの調整で、保存データやコピー内容の順序は変更なし。
	- 表示バージョンを `2.02` から `2.03` へ更新（+0.01）。

## 2026-08-04 14:21 JST
- v2.02
	- 完了履歴の通常タスク詳細を、`開始 14:15 / 中断 14:15 / 再開 14:16 / 完了 14:16` の横並びから、`　開始　14:15` のような縦並び・行頭インデント付き表示へ変更。
	- 完了履歴コピー文面も同じ縦並び・スペース間隔に合わせた。
	- 宿題・課題ブロックの表示形式、データ構造、完了保存、期限処理は変更なし。
	- 表示バージョンを `2.01` から `2.02` へ更新（+0.01）。

## 2026-08-04 14:20 JST
- v2.01
	- 完了履歴の通常タスク表示で、`開始 / 中断 / 再開 / 完了` の進行文を横並びから縦並びに変更。
	- 完了履歴コピー文面も通常タスク側のみ同様に縦並びへ変更し、表示とコピーの見え方を揃えた。
	- 宿題・課題ブロックの表示形式、データ構造、完了保存、期限処理は変更なし。
	- 表示バージョンを `2.00` から `2.01` へ更新（+0.01）。

## 2026-08-04 14:13 JST
- v2.00
	- 完了履歴画面を2ブロック構成へ変更し、`【完了履歴】`（historyEventsByDate由来）と `【宿題・課題】`（homeworkTasks由来）を分離表示するよう更新。
	- `【完了履歴】` には通常タスク（開始/中断/再開/完了）および各種チェック完了・薬完了のみを時系列で表示し、宿題・課題は表示しないよう変更。
	- `【宿題・課題】` は案件ごとにまとめて表示し、案件名見出し配下にチェック項目完了履歴（時刻順・`完了`文言なし）を表示するよう変更。
	- 宿題・課題の進行中表示は `次：先頭未完了項目` / `次：先頭未完了項目（他N項目）`、全体完了済みは `最終完了時刻 + 完了` を案件内に表示するよう変更。
	- 提出区分は宿題・課題ブロックでは表示しないよう変更し、期限表示は案件ごとに維持。
	- 「履歴をコピー」の文面も画面と同じ2ブロック構成へ変更。
	- historyEventsByDate の保存方法、homeworkTasks のデータ構造、チェック処理、期限管理、個別完了時刻保存、全体完了判定は変更なし。
	- 表示バージョンを `1.99` から `2.00` へ更新（+0.01）。

## 2026-08-04 13:26 JST
- v1.99
	- 宿題・課題の進行中表示文言を `残り：` 形式から `次：` 形式へ変更。
	- 残り1件は `次：項目名`、残り2件以上は `次：先頭未完了項目名（他N項目）` と表示するよう変更。
	- 先頭未完了項目（次にやる項目）を `次：` の表示対象とし、残り件数は括弧書きで表示。
	- 宿題・課題の動作、履歴記録、期限管理、その他の処理は変更なし。
	- 表示バージョンを `1.98` から `1.99` へ更新（+0.01）。

## 2026-08-04 13:22 JST
- v1.98
	- 宿題・課題の進行中表示を、`あと○項目` 形式から次にやる項目が分かる `残り：` 形式へ変更。
	- 残り1件の場合は `残り：項目名`、残り2件以上の場合は `残り：先頭未完了項目名 他N項目` を表示するよう変更。
	- 進行中表示は未完了項目が1件以上ある場合のみ表示し、残り0件では進行中表示を出さず従来どおり全体完了表示へ切り替わる挙動を維持。
	- 宿題・課題の履歴記録、期限管理、その他の処理は変更なし。
	- 表示バージョンを `1.97` から `1.98` へ更新（+0.01）。

## 2026-08-04 10:17 JST
- v1.97
	- 日付切替時の state 再生成で、前日の未完了宿題・課題（`done !== true`）を翌日へ引き継ぐ処理を追加。
	- 通常ロード時の日付切替経路と、Firebase同期データ読み込み後の日付切替経路の両方で同一の宿題引き継ぎ処理を適用。
	- 引き継ぎ対象は宿題データ全体を保持し、提出区分・期限・チェック状態・登録時刻・個別項目履歴など既存情報を維持。
	- `done === true` の宿題・課題は翌日の未完了一覧へ引き継がない仕様を維持。
	- 宿題・課題以外の日付切替処理は変更なし。
	- 表示バージョンを `1.96` から `1.97` へ更新（+0.01）。

## 2026-08-03 22:44 JST
- v1.96
	- 完了履歴の宿題・課題「全体完了」表示条件を調整し、完了時刻（`completedAtMs`）が保存されていないデータは全体完了行として表示しないよう変更。
	- これにより、`8/3 --:--　通常の提出　完了` および `8/3 --:--　書類提出・質問・確認（自宅での作業なし）　完了` のような時刻不明行を非表示化。
	- 項目ごとの完了履歴表示・進行中表示・通常の完了時刻付き全体完了表示は維持。
	- 表示バージョンを `1.95` から `1.96` へ更新（+0.01）。

## 2026-08-03 22:29 JST
- v1.95
	- 宿題・課題の提出チェック項目が `未完了→完了` へ変わった瞬間に、項目ごとの完了履歴イベントを保存する機能を追加。
	- 項目ごとの履歴は、完了履歴に `HH:mm　提出区分` + `項目名　完了` の2行ブロックとして当日分へ表示するよう追加。
	- 進行中表示（提出区分・完了済み項目・残り項目数・期限）は従来どおり維持し、同日に項目完了履歴と併記されるよう対応。
	- 全項目完了時の全体完了履歴（`M/D HH:mm　提出区分　完了`）は従来どおり表示し、同日に完了した個別項目履歴も保持。
	- チェックを外した場合でも過去の項目履歴は削除せず、再度 `未完了→完了` になった際は新しい履歴として追加されるよう対応。
	- 宿題・課題データに `submissionItemCompletionEvents`（項目名・提出区分・宿題名・完了時刻）を保存するよう追加。
	- 既存の宿題・課題チェックフローおよび期限管理の動作は変更なし。
	- 表示バージョンを `1.94` から `1.95` へ更新（+0.01）。

## 2026-08-03 22:21 JST
- v1.94
	- 予定入力画面で `明日` を選択したとき、毎回確認ダイアログ（`⚠ 確認`）を表示するよう変更。
	- ダイアログ本文を `明日（M/D）の予定を入力します。` とし、`よろしいですか？` を表示。
	- `はい` を押した場合のみ明日の予定入力へ切り替えるよう変更。
	- `キャンセル` を押した場合は編集日を変更せず、今日の予定入力画面に残るよう変更。
	- `今日` を選択する場合は確認ダイアログを表示しない既存動作を維持。
	- そのほかの予定入力画面の表示・保存・操作は変更なし。
	- 表示バージョンを `1.93` から `1.94` へ更新（+0.01）。

## 2026-08-03 22:13 JST
- v1.93
	- ホーム画面の表示文言を `持ち物/やること` から `持ち物・やること一覧` へ変更し、持ち物がない場合は `持ち物・やること一覧：なし` と表示するよう更新。
	- 完了履歴に `宿題・課題` の進捗表示を追加し、登録時刻・提出区分・完了済み項目・残り項目数（`あとN項目`）・期限（`期限：M/D`）を表示するよう変更。
	- 宿題・課題が完了状態の場合、進捗表示を置き換えて `M/D HH:mm　<提出区分>　完了` の1行表示にするよう変更。
	- 宿題・課題データに登録時刻・完了時刻を保持する項目を追加し、完了履歴表示とコピー内容に反映。
	- 表示バージョンを `1.92` から `1.93` へ更新（+0.01）。

## 2026-08-03 21:52 JST
- v1.92
	- ホーム画面の `持ち物` 表示ラベルを `持ち物/やること` へ変更。
	- 持ち物がない場合の表示も `持ち物/やること：なし` へ変更。
	- そのほかの表示・処理・レイアウトは変更なし。
	- 表示バージョンを `1.91` から `1.92` へ更新（+0.01）。

## 2026-08-03 21:43 JST
- v1.91
	- 予定入力画面の折りたたみレイアウトを調整し、`起床時間 / 出発時間 / 帰宅時間 / 勉強開始時間` を個別カード4枚から1枚のカードへ統合。
	- 上記4項目は統合カード内で従来どおり入力でき、見出しタップで展開・再タップで折りたたみできる仕様を維持。
	- 予定入力ページ内には表示されない `宿題・課題` 折りたたみカードを削除。
	- `登録済みタスク` より下のレイアウト・入力方法・保存処理は変更なし。
	- 表示バージョンを `1.90` から `1.91` へ更新（+0.01）。

## 2026-08-03 21:37 JST
- v1.90
	- 予定入力画面の `登録済みタスク` より上の項目を折りたたみ表示へ変更。
	- 折りたたみ対象: `起床時間` / `出発時間` / `帰宅時間` / `勉強開始時間` / `持ち物・やること` / `宿題・課題`。
	- 初期表示は見出しのみ表示し、見出しタップで展開、再タップで折りたたみできるよう変更。
	- `登録済みタスク` より下のレイアウト・入力方法・保存処理は変更なし。
	- 表示バージョンを `1.89` から `1.90` へ更新（+0.01）。

## 2026-08-03 12:37 JST
- v1.89
	- 部活後チェック②の文言を `自宅前で靴を脱ぎ、砂を落としてから家に入った` から `電柱付近で靴を脱ぎ、砂を落としてから家に入った` に変更。
	- 部活後チェックの順序、完了条件、親確認画面表示、履歴記録方法は変更なし。
	- 表示バージョンを `1.88` から `1.89` へ更新（+0.01）。

## 2026-08-03 12:31 JST
- v1.88
	- 部活後チェックの内容を5項目から8項目へ更新し、①②（帰宅前・家へ入る前の確認）が一覧先頭となる順序で固定。
	- 部活後チェック画面の見出し直下に、`気遣いとは、「自分が汚したもの・使ったもの・伝えるべきこと」を、自分で最後まで責任を持つことです。` の説明文を追加。
	- 8項目すべてをチェックボックスとして表示し、8項目すべてにチェックするまで `親に確認してもらう` を押せない既存制御を8項目基準へ拡張。
	- 親確認画面にも8項目の `☑ / □` 状態表示を維持し、案内文を `①〜⑧が終わっているか確認してください。` へ更新。
	- 親確認画面の `完了` 押下時のみ部活後チェック完了となる既存条件、完了履歴への記録方法、その他の部活後チェック動作は変更なし。
	- 通知や別画面の追加は行わず、同一画面・同一順序で繰り返し確認する運用を維持。
	- 表示バージョンを `1.87` から `1.88` へ更新（+0.01）。

## 2026-08-03 11:51 JST
- v1.87
	- 完了履歴画面で `今日は終了` を押したあと、終了確定前に `🟡 親への報告` ポップアップを表示するステップを追加。
	- ポップアップに `黒ノート改善事項の報告` と `今日一番気を付けたことの報告` の2チェックを追加し、2項目チェック完了まで `報告しました` を押せないよう変更。
	- `戻る` 押下で完了履歴画面へ戻り、`報告しました` 押下時のみ既存の `今日は終了` 確定処理（未完了確認→結果→1日終了フロー）を実行するよう変更。
	- 既存の履歴コピー処理、完了履歴保存、タスク実績、終了確定後の既存処理は変更なし。
	- 表示バージョンを `1.86` から `1.87` へ更新（+0.01）。

## 2026-08-03 10:09 JST
- v1.86
	- 部活後チェック①の文言を微修正し、`靴の中の靴下の泥や砂` を `靴の中や靴下の泥や砂` に変更。
	- 既存の遷移、完了条件、チェックボックス仕様は変更なし。
	- 表示バージョンを `1.85` から `1.86` へ更新（+0.01）。

## 2026-08-03 10:00 JST
- v1.85
	- 部活後チェック①の文言を `グラウンドで靴の中の靴下の泥や砂をできるだけ落とし、自宅前でもう一度確認して落とし切った` に変更。
	- 帰宅後報告画面の `コピーしてLINEを開く` 成功後、帰宅時チェックを完了確定したうえで、部活動の日が `はい` の場合はホームを経由せず直ちに部活後チェック画面へ遷移するよう変更。
	- 親確認画面の案内文を `帰宅時チェックの内容をＬＩＮＥ後、` + `①〜⑤が終わっているか確認してください。` へ変更。
	- 上記案内文の `帰宅時チェックの内容をＬＩＮＥ後、` を赤字表示に変更。
	- `親に確認してもらう` 押下では未完了のまま、`完了` 押下時のみ完了・履歴記録となる既存仕様は維持。
	- 表示バージョンを `1.84` から `1.85` へ更新（+0.01）。

## 2026-08-03 02:33 JST
- v1.84
	- 部活後チェックの5項目を表示専用テキストからチェックボックスへ変更し、各項目のチェック状態を当日分の状態として保存するよう追加。
	- `親に確認してもらう` ボタンは初期状態で無効化し、5項目すべてにチェックが付いたときだけ押せるよう変更。
	- 親確認画面に、本人がチェックした5項目の状態を `☑ / □` 付きでそのまま表示するよう変更。
	- `親に確認してもらう` 押下時は引き続き未完了のままとし、親確認画面の `完了` を押したときだけ部活後チェック完了・ホーム非表示・履歴記録する既存完了条件を維持。
	- 既存の部活後チェック表示条件、帰宅後チェック、通知、その他画面の仕様は変更なし。
	- 表示バージョンを `1.83` から `1.84` へ更新（+0.01）。

## 2026-08-03 02:22 JST
- v1.83
	- 帰宅時チェックの最後に、当日（現在日）のみ表示する質問 `今日は部活動の日でしたか？（はい / いいえ）` を追加。
	- `はい` を選んだ日の帰宅後チェック完了後、ホーム画面上部に `⚾ 部活後チェック（未完了）` を常時表示し、完了まで消えないよう追加。
	- 部活後チェック画面を追加し、5項目の確認内容と `親に確認してもらう` ボタンを実装。
	- 親確認画面 `⚾ 部活後チェック` を追加し、`完了` で当日完了・ホーム非表示・当日履歴記録、`戻る` で未完了のままホームへ戻るよう追加。
	- 部活後チェック状態は当日限定で保存し、翌日には持ち越さないよう日付単位で初期化されるよう追加。
	- 帰宅後チェックの既存項目・出発前チェック・宿題/課題・通知系の既存仕様は変更なし。
	- 表示バージョンを `1.82` から `1.83` へ更新（+0.01）。

## 2026-08-03 01:47 JST
- v1.82
	- 当日中に `実績時間60秒未満で完了` が1件でも発生した場合、当日終了まで有効な「開始確認モード」を日付単位で保存する `dailySupportByDate[YYYY-MM-DD].activeSupportId` を追加。
	- 今回のサポート項目として `秒が動いたことを確認してから始める`（ID管理）を実装し、開始確認モード中にタスク開始すると最優先の全画面「今日のサポート」を表示するよう変更。
	- 「今日のサポート」は実タスクタイマーと連動した経過時間表示（0秒/1秒/2秒...）を表示し、`確認しました` を押すまで通常実行画面へ進まないよう変更。
	- 予定時間到達時の通知後UIをインライン表示から最優先の全画面確認へ変更し、`予定時間になりました` / `完了` / `中断` / `もうちょっと` を表示するよう変更。
	- `もうちょっと` 押下時は通常実行画面へ戻し、`🟡 もう少し続けています` と `完了 / 中断` を実行画面上部に固定表示するよう変更。
	- `もうちょっと` 押下後20分の再確認到達時は、同様に最優先の全画面確認（`まだ続いていますか？`）を再表示するよう変更。
	- ホーム画面でも `もう少し続けています` の上部表示（タスク名:内容、`完了 / 中断`）を表示するよう変更。
	- 完了・中断時は既存どおり実行状態を初期化し、予定時間/再確認の全画面表示・継続表示・対象タスク再確認通知が解除される挙動を維持。
	- 通知音・バイブ・ローカル通知予約/取消・履歴保存・実績時間計算の既存処理は維持。
	- 表示バージョンを `1.81` から `1.82` へ更新（+0.01）。

## 2026-08-03 01:18 JST
- v1.81
	- 完了履歴画面の操作順を変更し、`今日は終了` の前に `履歴をコピー` ボタンを表示するよう変更。
	- 完了履歴をコピーできた場合のみ、画面上に `✅ コピーしました` を表示するよう追加。
	- `今日は終了` をコピー前に押した場合は、`まだ履歴をコピーしていません。` の確認ダイアログを表示し、`コピーする` / `そのまま終了` を選べるよう追加。
	- 既存の終了処理・保存・同期ロジックは変更なし。
	- 表示バージョンを `1.80` から `1.81` へ更新（+0.01）。

## 2026-08-02 23:07 JST
- v1.80
	- NotificationChannel 診断用に、現在端末へ登録されている全チャネルの `channelId / 表示名 / importance / sound URI / AudioAttributes.USAGE / vibration / vibrationPattern / bypassDnd` を一覧取得して表示できる機能を追加。
	- Task Finish Alert / Departure Alert / 20分後再確認 の各通知予約時に、実際に使用する `channelId / sound URI / AudioAttributes` をコンソールログへ出力するよう追加。
	- 実機で通知が鳴った直後に、受信イベントの `channelId` とイベント内容をコンソールログへ出力するよう追加。
	- 表示バージョンを `1.79` から `1.80` へ更新（+0.01）。

## 2026-08-02 22:03 JST
- v1.79
	- Departure Alert と Task Finish Alert の差分を比較し、固定音源の実URI解決タイミングだけを最小修正。
	- `ensureLocalNotificationChannel()` の直前で固定音源プリセットを必ずネイティブ解決してからチャネル生成するよう変更し、Departure でも Task Finish と同じアラーム音量系統の実URIを使ってチャネル作成するよう修正。
	- 既存の `フレッシュスタート` 選択状態、Task Finish 側の正常な鳴動、通知仕様は変更なし。
	- 表示バージョンを `1.78` から `1.79` へ更新（+0.01）。

## 2026-08-02 20:39 JST
- v1.78
	- 通知音の固定選択肢として `フレッシュスタート` と `バイシクル` を設定画面へ常時表示し、音選択画面を開かなくても選べるよう変更。
	- 既定値を `出発前=フレッシュスタート`、`タスク予定時間=バイシクル`、`20分後再確認=バイシクル` に変更。
	- 固定音源選択時の解決優先順位を `Pixel/端末対応音源URI -> アプリ同梱音源 -> 同種別のAndroid既定音` に対応。
	- Android 側にライセンス問題のない同梱音源 `fresh_start.wav` / `bicycle.wav` を追加し、端末音源が見つからない場合に利用するよう変更。
	- 固定音源でも実際に使うURI（または同梱音源）を channelId 生成に含め、旧チャネルを再利用しないよう維持。
	- 表示バージョンを `1.77` から `1.78` へ更新（+0.01）。

## 2026-08-02 19:57 JST
- v1.77
	- 起動時の通知チャネル整理処理を追加し、`listChannels` で取得した channelId を基準に、現在設定で使用していない古いチャネルのみ削除するよう変更。
	- 削除判定はチャネル名ではなく channelId で実施し、現在使用中の `General Alert` / 出発前 / タスク完了 / Recheck の最新 channelId は保持。
	- 削除処理は `deleteChannel` 経由で Android の `NotificationManager.deleteNotificationChannel()` を利用。
	- 表示バージョンを `1.76` から `1.77` へ更新（+0.01）。

## 2026-08-02 16:41 JST
- v1.76
	- タスク編集の「修正を保存」時に、保存処理内で編集中フォーム状態（mode/targetId/入力項目）を新規入力用へ初期化してから保存するよう変更。
	- これにより、修正保存完了後に入力欄へ編集内容が残り続ける状態を解消し、そのまま次の新規タスク入力へ移れるよう修正。
	- 新規タスク保存時の既存動作（並び順・通知・予定時間・保存仕様）は変更なし。
	- 表示バージョンを `1.75` から `1.76` へ更新（+0.01）。

## 2026-08-02 16:22 JST
- v1.75
	- Recheck通知IDをタスク完了通知IDと分離し、Recheck専用ID（新方式）で予約・取消するよう変更。
	- 旧Recheck通知ID（タスク完了通知IDを流用していた方式）は、新Recheck予約前と通知取消時に必ずキャンセルするよう変更。
	- アプリ起動時に pending 通知を確認し、旧Recheck通知ID（旧方式）に該当する `task-recheck` 通知が残っている場合は自動キャンセルする処理を追加。
	- Recheck時のみ旧オーバーラン即時発火経路（実行中経過秒で直接発火する経路）を停止し、新しいRecheck通知経路のみに統一。
	- 出発前通知・タスク完了通知の予約/発火仕様は変更なし。
	- 表示バージョンを `1.74` から `1.75` へ更新（+0.01）。

## 2026-08-02 16:15 JST
- v1.74
	- Recheckアラートの音設定を `notification` 扱いから `alarm` 扱いへ変更し、初期値・保存値正規化・音選択呼び出しを `alarm` に統一。
	- Recheckアラートの音選択UIを「通知音」表記から「アラーム音」表記へ変更し、Finishアラートと同じアラーム音一覧を開くよう変更。
	- Recheck通知チャネルは `toneType: alarm` に基づく `audioUsage: alarm` とアラーム用 channelId を使用するよう変更。
	- 表示バージョンを `1.73` から `1.74` へ更新（+0.01）。

## 2026-08-02 15:51 JST
- v1.73
	- `pickNotificationSound` の `catch` 内で `errorText` を組み立て、`console.error` と `alert` の両方を `RingtonePicker error: ` + 文字列1本で出力する形式へ変更。
	- 表示バージョンを `1.72` から `1.73` へ更新（+0.01）。

## 2026-08-02 15:48 JST
- v1.72
	- `pickNotificationSound` の `catch` で `console.error` を `JSON.stringify(error, Object.getOwnPropertyNames(error))` 出力へ変更。
	- `pickNotificationSound` の `catch` で、`alert` を一時的に同じ JSON 文字列を表示する形式へ変更。
	- 表示バージョンを `1.71` から `1.72` へ更新（+0.01）。

## 2026-08-02 15:44 JST
- v1.71
	- `pickNotificationSound` の `catch` で、例外をオブジェクト直出しせず、`message` と `stack` を `console.error` に出力する形式へ変更。
	- 表示バージョンを `1.70` から `1.71` へ更新（+0.01）。

## 2026-08-02 15:35 JST
- v1.70
	- `pickNotificationSound` で `Androidアプリで設定してください。` を表示する直前に、`RingtonePicker` の解決状態（`plugin`、`typeof plugin`、`typeof plugin.pickSound`、`window.Capacitor.Plugins` のキー一覧）を `console.error` 出力するよう追加。
	- `RingtonePicker` 取得処理を補強し、`window.Capacitor.Plugins.RingtonePicker` を優先参照し、`registerPlugin("RingtonePicker")` で取得できたプロキシを `window.Capacitor.Plugins.RingtonePicker` に反映するよう変更。
	- `pickNotificationSound` は `window.Capacitor.Plugins.RingtonePicker` を含む解決済みプラグインへ直接 `pickSound` を呼び出すよう変更。
	- 表示バージョンを `1.69` から `1.70` へ更新（+0.01）。

## 2026-08-02 15:04 JST
- v1.69
	- `pickNotificationSound` から `bridge.isNativePlatform()` による分岐を削除し、`RingtonePicker` プラグイン取得後は `plugin.pickSound` を直接呼び出すよう変更。
	- `Androidアプリで設定してください。` は、`RingtonePicker` プラグインが取得できない場合と `plugin.pickSound` が存在しない場合のみ表示するよう変更。
	- 例外発生時は `console.error` に出力するよう変更。
	- 表示バージョンを `1.68` から `1.69` へ更新（+0.01）。

## 2026-08-02 15:04 JST
- v1.68
	- 表示バージョンを `1.67` から `1.68` へ更新（+0.01）。

## 2026-08-02 13:46 JST
- v1.67
	- タスク予定時間通知の音種別を通常通知からアラームへ変更し、保存初期値・既存保存値正規化・音選択画面を `alarm` 用途へ統一。
	- 既存の `taskFinish` 保存値が `notification` の場合でも、選択済み URI と音タイトルを維持したまま `alarm` へ移行し、新しい `task-finish-alarm-<識別子>` 形式の channelId を再生成するよう変更。
	- 設定画面の 3 通知それぞれに `試しに鳴らす` と `停止` を追加し、保存済みシステム音 URI を直接再生できるよう変更。
	- `RingtonePickerPlugin` に `previewSound` / `stopPreview` を追加し、アラーム用途は `USAGE_ALARM`、再確認通知は `USAGE_NOTIFICATION` で再生するよう対応。
	- 試し鳴らし中の音は、別の音を再生したとき・停止ボタン押下時・設定画面を閉じたとき・プラグイン破棄時に停止するよう変更。
	- 通知予約ID、通知取消、出発前通知と再確認通知の音種別・通知本文・通知時刻は変更なし。
	- 表示バージョンを `1.66` から `1.67` へ更新（+0.01）。

## 2026-08-02 12:35 JST
- v1.66
	- 通知音設定データを通知種類ごとに拡張し、`通知種類 / 音タイトル / 音URI / alarm・notification種別 / 使用channelId` を保存するよう変更。
	- 出発前通知・タスク予定時間通知・20分後再確認通知の各設定で、選択した URI から短い識別子を作り、`departure-alarm-<識別子>` / `task-finish-sound-<識別子>` / `task-recheck-sound-<識別子>` 形式の channelId を生成して利用する方式へ変更。
	- 固定チャネルID利用を廃止し、予約時は通知種類ごとに保存済み channelId を参照して送信するよう変更。
	- Android 8+ のチャネル音変更制約に対応するため、音設定変更時は該当チャネルを安全に再作成する処理へ更新。
	- 端末固有 URI が別端末で無効な場合に備え、ネイティブ側で URI 解決失敗時は既定アラーム音/既定通知音へフォールバックする処理を追加。
	- `node_modules` 直編集依存を避けるため、`@capacitor/local-notifications` を `plugins/local-notifications` のローカル依存（`file:`）へ切り替えて恒久化。
	- ブラウザ版で音選択を押した場合は、`Androidアプリで設定してください。` と表示するよう変更。
	- 表示バージョンを `1.65` から `1.66` へ更新（+0.01）。

## 2026-08-02 12:18 JST
- v1.65
	- 設定画面に、通知種類ごとの通知音選択機能（Android）を追加。
	- 対象は `出発前通知`（アラーム音一覧）、`タスク予定時間通知`（通知音一覧）、`20分後の再確認通知`（通知音一覧）の3種類。
	- Android の `RingtonePicker` を呼び出すカスタムネイティブプラグインを追加し、端末内の候補一覧表示・試聴・選択に対応。
	- 選択結果は通知種類ごとに `state.notificationSoundSettings` としてユーザーID単位の保存データへ保持。
	- 選択した音名を設定画面に表示するよう変更。
	- 3通知をチャネル分離し、選択URIをチャネル音へ反映するよう Local Notifications の Android チャネル生成処理を拡張（URI指定 / `audioUsage` 指定対応）。
	- `タスク予定時間通知` と `20分後の再確認通知` を通知ソースとして分離し、個別音設定を反映。
	- 表示バージョンを `1.64` から `1.65` へ更新（+0.01）。

## 2026-08-02 11:52 JST
- v1.64
	- Android のローカル通知チャネルを用途別に分離し、予定時間到達通知・20分後再確認通知・出発前通知の 3 種類を専用チャネル `task-alarm-alert-v1` に切り替え。
	- 上記 3 通知専用チャネル作成時に、ネイティブ側で `RingtoneManager.TYPE_ALARM` の端末既定アラーム音 URI を設定する経路を追加。
	- 上記 3 通知専用チャネルの `AudioAttributes` を `USAGE_ALARM` に設定し、通知音の再生ストリームをアラーム用途に最適化。
	- 上記 3 通知専用チャネルに約 5 秒の振動パターン（約 `0.5` 秒振動 / 約 `0.3` 秒停止の繰り返し）を適用。
	- 既存の通知予約・キャンセル・notificationId 管理ロジックは変更せず再利用。
	- 服薬通知・帰宅通知・テスト通知は既存の一般通知チャネル（`task-alert-v2`）を継続利用し、挙動を変更しない。
	- 表示バージョンを `1.63` から `1.64` へ更新（+0.01）。

## 2026-08-02 10:51 JST
- v1.63
	- 予定時間到達通知、20分後の再確認通知、出発前通知の 3 種類を同じ通知設定に統一。
	- 振動は約 `0.5` 秒振動 / 約 `0.3` 秒停止の繰り返し、通知音は現状維持。
	- Android の既存 NotificationChannel 設定が残る端末を考慮し、通知チャネル ID を更新して旧チャネルを削除したうえで再作成するよう変更。
	- 表示バージョンを `1.62` から `1.63` へ更新（+0.01）。

## 2026-08-02 10:48 JST
- v1.62
	- 予定時間到達時の振動を、約 `0.5` 秒振動 / 約 `0.3` 秒停止の繰り返しパターンに変更。
	- Android の通知チャネルは振動有効のまま維持し、Local Notifications の受信イベントでも同じ振動パターンを再生する補助経路を追加。
	- 通知音は変更せず、振動のみを更新。
	- 実機 Android では通知チャネル側の振動と WebView 側の `navigator.vibrate` を併用し、表示時の振動継続を強化。
	- 表示バージョンを `1.61` から `1.62` へ更新（+0.01）。

## 2026-08-01 23:55 JST
- v1.61
	- ホーム表示日キーと当日キー（JST）の比較に、当日終了状態を組み合わせた操作可否判定を追加。
	- 前日表示では `タスク実行` を不可のまま維持しつつ、`完了履歴を見る` を再び開けるように修正。
	- 当日表示では通常時に `タスク実行 / 宿題・課題 / 完了履歴` をすべて許可。
	- 当日表示で「全タスク完了」または「今日は終了」状態になった後は、`タスク実行 / 宿題・課題` を不可、`完了履歴` は引き続き許可するよう変更。
	- 翌日表示では `タスク実行` と `完了履歴` を不可、`宿題・課題` は許可するよう変更。
	- `changePhase` に最終ガードを追加し、ボタン状態に関係なく前日/翌日の実行遷移、翌日の完了履歴遷移、終了済み当日の実行/宿題遷移を拒否。
	- 完了履歴表示の対象日を表示コンテキストに合わせて解決し、前日表示中は前日履歴を閲覧できるよう修正。
	- 既存の完了履歴データ構造と「今日は終了」の処理内容は変更なし。
	- 表示バージョンを `1.60` から `1.61` へ更新（+0.01）。

## 2026-08-01 23:46 JST
- v1.60
	- 表示中の日付キーと今日の日付キーを比較し、表示日が当日のときだけタスク実行を許可する判定を追加。
	- 未来日表示中はホームの `タスク実行へ` ボタンを無効化するよう変更。
	- 未来日表示中はホームのタスクカードを操作不可にし、カード押下で実行画面へ遷移しないよう変更。
	- 実行開始処理 `startTask` に当日判定ガードを追加し、未来日のカード操作経由で開始処理が走らないよう保護。
	- 予定報告画面でも表示対象が未来日の場合は `タスク実行へ進む` を無効化し、翌日予定入力中の実行遷移を防止。
	- 日付切り替わり後、表示日キーが当日キーと一致した時点で自動的に実行可能になる挙動に対応（キー比較判定）。
	- 予定入力、定期予定追加、保存、編集、削除の既存機能には影響なし。
	- 表示バージョンを `1.59` から `1.60` へ更新（+0.01）。

## 2026-08-01 23:34 JST
- v1.59
	- タスクの延長機能（10分/20分/任意延長、通知停止、延長中表示）を廃止。
	- 予定時間到達時の通知音・バイブは維持し、実行画面に `予定時間になりました。` と `完了 / 中断 / もう少し続ける` の3択確認を表示する方式へ変更。
	- `もう少し続ける` で警告表示を閉じ、通常の実行画面（完了/中断のみ）へ戻るよう変更。
	- `もう少し続ける` 押下時に既存の終了通知をキャンセルし、20分後の再確認通知を再予約するよう変更。
	- 20分後も同一タスク実行中の場合、通知音・バイブを再作動し、ホーム最上部に `⚠ まだ続いていますか？` の確認欄を表示するよう追加。
	- 確認欄の表示文言を `タスク名：内容`（内容未入力時はタスク名のみ）に対応。
	- 完了操作をモーダル確認方式へ変更し、`完了しますか？ / 完了する / 戻る` のみ表示。外側タップでは閉じない仕様に変更。
	- 完了または中断時に、再確認通知・確認欄・超過警告状態を解除する動作を適用。
	- 既存の開始処理、実績時間計算、中断/再開、完了履歴記録は維持。
	- 表示バージョンを `1.58` から `1.59` へ更新（+0.01）。

## 2026-08-01 22:51 JST
- v1.58
	- 延長中ラベル（例: `10分延長中`）の表示位置を調整し、`予定時間` のすぐ右側に表示するよう変更。
	- ラベルは右方向へ約 `1cm` オフセットして表示。
	- 既存の赤太字スタイルと、延長中のみ表示される条件は維持。
	- 表示バージョンを `1.57` から `1.58` へ更新（+0.01）。

## 2026-08-01 22:50 JST
- v1.57
	- タスク実行中の延長中ラベル（例: `10分延長中`）の表示位置を調整し、経過時間と同じ行の中央に表示するよう変更。
	- 赤太字スタイルと延長中のみ表示する条件は維持。
	- 表示バージョンを `1.56` から `1.57` へ更新（+0.01）。

## 2026-08-01 22:48 JST
- v1.56
	- タスク実行中に延長が有効な間、経過時間表示を `○分○秒` 形式から `予定分＋超過分` 形式へ変更。
	- 例: 予定30分で延長中は、時間表示を `30分＋○分○秒` として表示。
	- 延長していない通常時の経過表示（`○分○秒`）は維持。
	- 既存の延長中ラベル（赤太字・右側表示）は維持。
	- 表示バージョンを `1.55` から `1.56` へ更新（+0.01）。

## 2026-08-01 22:45 JST
- v1.55
	- タスク実行中の経過時間表示行を調整し、延長中ラベルを時間表示の右側に並べて表示するレイアウトへ変更。
	- 延長中ラベル（例: `10分延長中`）を赤太字で強調表示するスタイルを追加。
	- 表示条件（延長中のみ表示、終了時刻到達で非表示）は既存仕様を維持。
	- 表示バージョンを `1.54` から `1.55` へ更新（+0.01）。

## 2026-08-01 22:35 JST
- v1.54
	- 前日未完了判定と持ち越し判定の対象日取得ロジックを見直し、`targetDateKey` を最優先、なければ `recurringDateKey` を使う仕様へ統一。
	- `targetDateKey` と `recurringDateKey` の両方がない既存タスクは、前日未完了・持ち越し判定の対象外に変更。
	- これにより、日付欠損タスクが `prevDateKey` 扱いで誤って前日未完了へ入る問題を解消。
	- 未来日タスクは従来どおり前日未完了に含めず、前日以前の日付を持つ対象のみ集計・判定。
	- `normalizeTask` の互換処理は変更せず、未完了判定側のみを修正。
	- 表示バージョンを `1.53` から `1.54` へ更新（+0.01）。

## 2026-08-01 22:32 JST
- v1.53
	- 日付切替時の未完了判定に日付条件を追加し、`targetDateKey` が前日以前のタスクだけを前日集計の対象にするよう修正。
	- これにより、前日中に作成した翌日タスク（未来日 `targetDateKey`）は「前日未完了」に含まれないよう改善。
	- 持ち越し生成条件を見直し、`deferred` かつ `targetDateKey` が前日以前のタスクのみ翌日に持ち越すよう変更。
	- 未来日の予定は未完了扱いせず、予定日 0:00 以降に当日予定として扱う動作へ統一。
	- 表示バージョンを `1.52` から `1.53` へ更新（+0.01）。

## 2026-07-30 16:15 JST
- v1.52
	- 任意延長分の表示を実値ベースで統一し、`7分延長` なら `7分延長中`、`15分延長` なら `15分延長中` を表示する仕様を維持・明確化。
	- 10分/20分延長も同一処理で `10分延長中` / `20分延長中` を表示。
	- 延長実行時に、当該タスクのローカル終了通知をいったんキャンセルしてから、延長後の終了時刻で再予約する処理を追加。
	- ローカル通知再予約の残り秒計算を、実行中は `baseSeconds` ではなく実経過秒で行うよう補正し、延長時刻ずれを防止。
	- `〇分延長中` は、再通知作動時・タスク完了時に消える既存条件を維持し、再延長時は新しい分数で上書き表示。
	- 表示バージョンを `1.51` から `1.52` へ更新（+0.01）。

## 2026-07-30 16:15 JST
- v1.51
	- タスク実行の超過延長（10分/20分/任意分）を実行した後、実行画面内に1行だけ `○分延長中` を表示するよう対応。
	- 表示文言は実行した延長分に合わせて `10分延長中` / `20分延長中` / `○分延長中` を自動表示。
	- 延長終了時刻（`alertAtSeconds` 到達）で再通知が作動したタイミングで、`○分延長中` 表示を自動で消去するよう対応。
	- タスク完了時（`state.running` 初期化）にも `○分延長中` 表示が残らないよう対応。
	- 既存の再通知音トリガー・延長後の通知ロジックそのものは変更なし。
	- 表示バージョンを `1.50` から `1.51` へ更新（+0.01）。

## 2026-07-30 13:00 JST
- v1.45
	- 将来の完了履歴タイムラインの共通データ源として、保存専用のイベントログ `historyEventsByDate` を追加。
	- 既存データ互換を維持するため、履歴イベント正規化と登校/帰宅チェック完了時刻補助フィールドの正規化を実装。
	- 共通イベントログへの保存を次の8操作に接続。
		- task: 開始 / 中断 / 再開 / 完了
		- check: 登校チェック完了 / 帰宅チェック完了
		- medicine: 青の薬完了 / 赤の薬完了
	- 薬の既存 `doneAt` 保存仕様は維持しつつ、同時に共通イベントログへ完了イベントを記録するよう対応。
	- 表示バージョンを `1.44` から `1.45` へ更新（+0.01）。

## 2026-07-30 13:29 JST
- v1.50
	- 完了履歴のタスク先頭行表示を調整し、`タスク名：内容` を1行表示に変更。
	- 内容未入力時は `タスク名` のみ表示するよう対応（不要な見出し・ラベルは追加しない）。
	- 開始/中断/再開/完了のインデント表示は維持。
	- `historyEventsByDate` の保存構造は変更なし（表示ロジックのみ変更）。
	- 表示バージョンを `1.49` から `1.50` へ更新（+0.01）。

## 2026-07-30 13:27 JST
- v1.49
	- 完了履歴のタスクブロック化ロジックを調整。
	- `taskId` がない場合は `taskName` ではまとめず、`task_started`（開始イベント）ごとに新しいブロックを作成する方式へ変更。
	- `task_paused` / `task_resumed` / `task_completed` は、その開始イベントで作成されたブロックへ紐付ける表示に変更。
	- これにより、同名タスクを1日に複数回実行しても別ブロックで表示されるよう対応。
	- `historyEventsByDate` の保存構造・保存順序は変更なし（表示ロジックのみ変更）。
	- 表示バージョンを `1.48` から `1.49` へ更新（+0.01）。

## 2026-07-30 13:23 JST
- v1.48
	- 完了履歴画面の表示をカード形式からテキスト一覧形式へ変更（枠・カード表示を廃止）。
	- 表示順は `occurredAtMs` 昇順を維持。
	- タスク系イベント（開始/中断/再開/完了）は、同一タスクごとに1ブロックへ集約して表示するよう変更。
	- タスク名はブロック先頭に1回のみ表示し、配下に `開始/中断/再開/完了` をインデント表示。
	- 出発前チェック・帰宅後チェック・青/赤の薬は1行表示を維持。
	- 保存構造 `historyEventsByDate` は変更なし（表示時のみグルーピング）。
	- 表示バージョンを `1.47` から `1.48` へ更新（+0.01）。

## 2026-07-30 13:11 JST
- v1.47
	- ホーム画面の導線を変更し、`1日の終了` ボタンを `完了履歴を見る` ボタンへ差し替え。
	- 新規 `完了履歴` 画面を追加。
	- 表示対象は当日 `dateKey` の `historyEventsByDate[dateKey]` のみとし、`occurredAtMs` 昇順でタイムライン表示。
	- 表示イベントは次の8種類に限定（他イベントは非表示）。
		- `check_departure_completed`
		- `check_return_completed`
		- `medicine_blue_completed`
		- `medicine_red_completed`
		- `task_started`
		- `task_paused`
		- `task_resumed`
		- `task_completed`
	- 完了履歴が0件の場合は `今日はまだ完了履歴がありません。` を表示。
	- 既存の `今日は終了` 処理内容は変更せず、ボタン配置を完了履歴画面最下部1か所へ移動。
	- 表示バージョンを `1.46` から `1.47` へ更新（+0.01）。

## 2026-07-30 13:08 JST
- v1.46
	- `historyEventsByDate` を日付切替時にも引き継ぐよう修正し、過去日履歴が失われない保存構造へ変更。
	- `createInitialState` に履歴引き継ぎ引数を追加し、`loadState` / `normalizeLoadedState` の日付不一致時再構築でも履歴を保持するよう対応。
	- `resetDailyStatus` 実行時に `historyEventsByDate` を削除しないよう変更（履歴は保持し、当日タスク/チェック/実行状態のみ初期化）。
	- `historyEventsByDate` は `YYYY-MM-DD -> イベント配列` 構造を正規化して維持。
	- 表示バージョンを `1.45` から `1.46` へ更新（+0.01）。

## 2026-07-30 12:40 JST
- 報告用タスク名の共通関数 `getReportedTaskName(task)` を追加。
- `recurringPlanId` が truthy のタスクのみ、報告用表示で先頭に `★` を付与するよう対応。
- 適用箇所は次の2か所に限定。
	- 最後の実績確認画面（`renderResult`）
	- 1日の終了時にコピーされる報告文（`buildResultReportText`）
- `recurringPlanId` がない手入力タスク・既存タスクには `★` を付与しない仕様を維持。
- ホーム画面・予定入力画面・タスク実行画面・その他表示には `★` を出さないまま維持。
- 表示バージョンを `1.43` から `1.44` へ更新（+0.01）。

## 2026-07-30 12:33 JST
- 薬カード内テキスト（青/赤の薬名・状態表示）を白太字へ統一して視認性を強化。
- 薬カードの `飲んだ` / `取り消し` ボタン配色を黄色系から淡いグリーン系グラデーションへ変更。
- ボタンの枠線・影もグリーン系に合わせて調整し、全体のトーンを統一。
- 表示バージョンを `1.42` から `1.43` へ更新（+0.01）。

## 2026-07-30 12:31 JST
- 薬リマインダーの青/赤カード配色を調整し、原色感を抑えたキラキラ系グラデーションへ更新。
- 青カード・赤カード内の `青の薬` / `赤の薬` および `未完了` 表示を白文字で強調し、視認性を向上。
- カード面にハイライト（光沢）表現を追加。
- `飲んだ` / `取り消し` ボタンの黄色系配色もグラデーション＋ハイライトへ変更し、目立ちやすさを改善。
- 表示バージョンを `1.41` から `1.42` へ更新（+0.01）。

## 2026-07-30 12:28 JST
- 薬リマインダーの青カード・赤カードを、枠色だけでなく背景色つきデザインへ変更。
- 青カードは青系グラデーション、赤カードは赤系グラデーションを適用。
- カード内の見出し・状態テキストを白文字へ統一し、視認性を改善。
- `飲んだ` / `取り消し` ボタンを目立つ高コントラスト配色（黄系）へ調整。
- 表示バージョンを `1.40` から `1.41` へ更新（+0.01）。

## 2026-07-30 12:19 JST
- 薬リマインダー機能を追加（青の薬・赤の薬を独立管理）。
- 毎日18:00開始で、未完了時は15分おきにローカル通知を再予約する処理を実装。
- 通知からアプリを開いた際に薬確認オーバーレイを表示する連携（`localNotificationActionPerformed`）を追加。
- 全画面オーバーレイを実装し、`飲んだ` / `取り消し` / `15分後` を提供。両方完了まで閉じられない仕様に対応。
- 日付切替時に薬状態を自動リセットする正規化ロジックを追加し、当日の完了時刻を保存。
- ホーム画面に `💊 今日の薬` サマリーを追加し、未完了時はタップでオーバーレイを開けるよう対応。
- 既存のタスク・宿題機能とは独立した状態管理とし、相互依存しないよう実装。
- 表示バージョンを `1.39` から `1.40` へ更新（+0.01）。

## 2026-07-29 17:21 JST
- 学習時間合計の表示形式を調整し、`60分を超えた場合のみ` `○時間○分` 表示に変更。
- `60分以下` は従来どおり `○分` 表示を維持。
- 対象は予定入力画面の合計表示と、親への予定報告（報告文）双方に適用。
- 表示バージョンを `1.38` から `1.39` へ更新（+0.01）。

## 2026-07-29 17:19 JST
- 予定入力画面の内容補助文を入力欄の外から削除し、枠内のグレー文字（placeholder）へ移動。
- placeholder文言を `あとで見返して何をやるか分かるように書こう` に更新。
- 位置移動に伴い未使用になった `helper-small` スタイルを削除。
- 表示バージョンを `1.37` から `1.38` へ更新（+0.01）。

## 2026-07-29 17:16 JST
- 予定入力画面の内容補助文 `あとで見返したときに / 何をやるか分かるように書こう` の位置を調整。
- 補助文を `内容` ラベル直下へ移動（入力欄の前に表示）し、案内の視認位置を統一。
- 表示バージョンを `1.36` から `1.37` へ更新（+0.01）。

## 2026-07-29 17:15 JST
- 予定入力画面の「内容」欄で、`あとで何をやるか分かるように` の案内を小さめフォントで2行表示に変更。
- `内容` 入力直下に補助文（`あとで見返したときに / 何をやるか分かるように書こう`）を追加。
- 補助文専用スタイル `helper-small` を追加し、通常の補助文より小さい文字サイズへ調整。
- 表示バージョンを `1.35` から `1.36` へ更新（+0.01）。

## 2026-07-29 17:10 JST
- 予定入力画面の `予定を作る日` ラベルを削除。
- ラベル削除後の余白が増えないよう、表示間隔は詰めた状態を維持。
- 表示バージョンを `1.34` から `1.35` へ更新（+0.01）。

## 2026-07-29 17:09 JST
- 予定入力画面の見出し直下に表示していた `内容は任意です。あとで見返して分かる書き方がおすすめです。` を削除。
- 該当文言削除に伴い、見出し下の余白が増えないようレイアウト上の空行も除去。
- 表示バージョンを `1.33` から `1.34` へ更新（+0.01）。

## 2026-07-29 17:00 JST
- タスク名オートコンプリート候補の並び順を、使用回数順から「最近使った順（lastUsedAt降順）」へ変更。
- タスク名候補の表示件数を最大10件に制限。
- 自由入力した新しいタスク名を `追加/修正` 保存時に即時学習し、次回入力時の候補へ自動反映するよう変更。
- 既存の単一入力欄 + datalist 方式は維持し、旧形式データとの互換性も維持。
- 表示バージョンを `1.32` から `1.33` へ更新（+0.01）。

## 2026-07-29 16:37 JST
- 予定入力のタスク登録UIを簡素化し、`タスク名` と `新しいタスク名` を統合して単一入力欄へ変更。
- タスク名プルダウンを廃止し、過去の登録名を入力候補として表示するオートコンプリート（datalist）へ変更。
- 候補選択と自由入力の両方を維持し、保存時は単一のタスク名入力を優先して処理するよう更新。
- 既存データ互換のため、旧保存形式（`taskNameChoice` / `customTaskName`）読み込み時は新しい単一入力へ自動補完。
- 表示バージョンを `1.31` から `1.32` へ更新（+0.01）。

## 2026-07-29 16:25 JST
- 宿題・課題の `作業あり` / `作業なし` を、時間付きタスク生成有無ではなく「課題の性質区分」として扱う仕様へ変更。
- `作業あり` 登録時に行っていた時間付きタスク（`${課題名}（作業）`）の自動作成を廃止。
- 提出確認フローの完了判定から `homeworkId` 紐づけ作業タスク依存を削除し、手動確認項目のみで完了するよう変更。
- これにより、作業あり/なしのどちらでも登録後は「黒の手帳 → 確認画面」運用で終了し、時間付きタスク作成画面へは自動遷移しない構成へ統一。
- 表示バージョンを `1.30` から `1.31` へ更新（+0.01）。

## 2026-07-29 15:30 JST
- 宿題・課題の編集画面から `完了状態` 表示を削除。
- `未完了` / `完了` ラジオボタンを削除し、完了状態の手動切り替えUIを非表示化。
- 表示バージョンを `1.29` から `1.30` へ更新（+0.01）。

## 2026-07-29 15:30 JST
- 宿題・課題の内容欄補助文を更新。
- `「何を」「どうする」まで書くと分かりやすくなります。` を、`あとで見返したときに「何を」「どうする」が分かるように書こう` へ変更。
- 表示バージョンを `1.28` から `1.29` へ更新（+0.01）。

## 2026-07-29 15:27 JST
- 時間付きタスクの内容欄補助表示を更新し、見出しを `何を書く？` から `書き方のコツ` へ変更。
- 補助例を指定内容へ差し替え（`新中学問題集 p54～59`、`学校プリント No.3`、`英検単語 Day12`、`数学 新A 31～40番`、`教科書 Unit4 リスニング`、`理科 レンズ実験レポート`）。
- 表示バージョンを `1.27` から `1.28` へ更新（+0.01）。

## 2026-07-29 15:20 JST
- 入力補助の確認条件を最終仕様へ調整し、文字数判定は廃止したまま維持。
- 確認ダイアログ表示条件を次の3条件に統一。
	- 内容が未入力
	- 内容が指定語（`問題集` / `プリント` / `教科書` / `ワーク` / `宿題` / `提出` / `確認` / `質問` / `勉強` / `やる` / `ノート` / `テスト`）と完全一致
	- 内容が項目名・タスク名と完全一致（前後空白は除去して比較）
- 確認ダイアログ文面を更新（見出し `内容の確認`、本文 `この内容で保存しますか？` など）し、例文表示を追加。
- 時間付きタスクの内容欄に `何を書く？` ガイドと詳細見本を追加。
- 宿題・課題の内容欄に `「何を」「どうする」まで書く` ガイドと詳細見本を追加。
- 保存禁止は行わず、確認ダイアログの `このまま保存` でそのまま保存できる仕様を維持。
- 表示バージョンを `1.26` から `1.27` へ更新（+0.01）。

## 2026-07-29 14:49 JST
- 入力補助の確認ダイアログ判定を、文字数基準から条件一致基準へ変更。
- 確認ダイアログ表示条件を次の3つに統一。
	- 内容が未入力
	- 内容が指定語（`問題集` / `プリント` / `教科書` / `ワーク` / `宿題` / `提出` / `確認` / `質問` / `勉強` / `やる` / `ノート` / `テスト`）のいずれかと完全一致
	- 内容が項目名（宿題名またはタスク名）と完全一致
- 文字数による短文判定（`○文字未満`）を廃止。
- 保存禁止は行わず、確認ダイアログの `このまま保存` でそのまま保存できる仕様を維持。
- 表示バージョンを `1.25` から `1.26` へ更新（+0.01）。

## 2026-07-29 14:34 JST
- 入力品質向上のため、宿題・課題と時間付きタスクの内容欄に入力補助文とプレースホルダーを追加。
- 宿題・課題の内容欄を任意入力に変更し、未入力でも保存できるよう調整。
- 時間付きタスクの内容欄も未入力で保存可能に変更し、保存禁止を行わない仕様へ統一。
- 宿題・課題 / 時間付きタスクの保存時に、内容が未入力または短い場合のみ1回確認ダイアログを表示。
- 確認ダイアログは `このまま保存` / `修正する` の2択で、ユーザー判断でそのまま保存可能。
- 判定は文字数のみを使い、語句の正しさや内容評価（AI判定）は行わない。
- 表示バージョンを `1.24` から `1.25` へ更新（+0.01）。

## 2026-07-29 14:24 JST
- 提出確認画面の標準フロー表示を修正し、`作業` をオーバーレイ表示から削除。
- 作業あり課題でも提出確認画面には手動確認項目（`黒の手帳へ書いた` / `カバンへ入れた` / `提出した` / `報告した`）のみ表示するよう変更。
- 標準フローの完了条件は維持し、作業ありではホーム画面の時間付きタスク完了（`homeworkId` 紐づけ）を引き続き完了判定に使用。
- 提出確認画面の標準フローを縦方向レイアウトへ変更し、各項目間に `↓` を表示して流れを明確化。
- 表示バージョンを `1.23` から `1.24` へ更新（+0.01）。

## 2026-07-29 13:50 JST
- 宿題・課題の標準提出フローを `homeworkWorkType` 基準へ刷新。
- 登録入口に `作業あり` / `作業なし` 分岐を追加し、保存時に `homeworkWorkType`（`with-work` / `no-work`）を保持。既存データは `with-work` 扱いで後方互換を維持。
- 提出テンプレート表示を読み取り専用化（作業あり: `通常の提出`、作業なし: `書類提出・質問・確認（自宅での作業なし)`）。
- 作業ありでは時間付き作業タスクを `homeworkId` 紐づけで自動作成し、`作業` ステップは紐づくタスク完了状態から自動判定。
- 作業なしは時間付き作業タスクを作成しない。
- 標準フローの順番制御を実装（次の1項目のみ操作可、後続は操作不可、確定済みは戻せない）。
- 手動項目は押下直後に確認ダイアログで確定方式へ変更し、誤操作時は保存前に取り消し可能化。
- すべての必要ステップ確定時に宿題本体を自動完了へ更新（描画中ではなく状態更新時に実行）。
- 表示バージョンを `1.22` から `1.23` へ更新（+0.01）。

## 2026-07-29 13:23 JST
- 宿題・課題の登録入口に `作業あり` / `作業なし` 分岐を追加し、詳細入力前に選択できるように変更。
- 宿題データへ `homeworkWorkType`（`with-work` / `no-work`）を追加。既存データに項目がない場合は `with-work` 扱いで後方互換を維持。
- `作業なし` 選択時は `submissionTemplateId` を `no-homework-submission` に自動設定し、提出テンプレート表示を固定。
- `作業なし` 判定を `homeworkWorkType === "no-work"` に統一し、提出確認4段階フローの表示順・操作制御・自動完了条件は既存仕様を維持。
- `作業なし` で宿題登録しても今日の時間付きタスクには追加しないことを確認（既存挙動維持）。
- 表示バージョンを `1.21` から `1.22` へ更新（+0.01）。

## 2026-07-29 12:59 JST
- `書類提出・質問・確認` 系の対象判定をテンプレート名比較から削除し、`submissionTemplateId` 起点の判定へ変更。
- `作業なし` / `作業無し` などテンプレート名文字列比較ロジックを削除。
- 4項目順制御・自動完了は既存 `submissionCheckedItemIds` を使う仕様のまま維持。
- 表示バージョンを `1.20` から `1.21` へ更新（+0.01）。

## 2026-07-29 12:55 JST
- `書類提出・質問・確認（自宅での作業なし）` テンプレートの4項目チェック操作を順番固定へ修正。
- 一度チェック済みになった項目は `✓` 表示のまま操作不可、未完了へ戻せない動作に変更。
- 現在進行中の次の1項目のみ操作可にし、後続項目は `○` 表示で操作不可に変更。
- `submissionCheckedItemIds` をそのまま利用し、新規保存項目は追加なし。自動完了条件は変更なし。他テンプレート動作には影響なし。
- 表示バージョンを `1.19` から `1.20` へ更新（+0.01）。

## 2026-07-29 12:49 JST
- 宿題・課題の `提出まであと○日` 計算を、実時計日ではなくアプリの表示基準日（ホーム表示日）で判定するよう修正。
- これにより `7/30締切・表示日7/30` で `今日提出`、表示日が締切超過日なら従来どおり `提出期限超過` を表示。
- 提出日前日判定ロジック（未完了チェック表示条件）は変更なし。
- 表示バージョンを `1.18` から `1.19` へ更新（+0.01）。

## 2026-07-29 12:38 JST
- 実機で `書類提出・質問・確認` の未完了手順が出ない問題を修正。テンプレート名の表記ゆれ（`自宅での作業なし` / `自体作業無し`）を同一対象として判定するように改善。
- 1段目手順の文言ゆれ（`黒の手帳へ書いた` / `黒の手帳へ記録した`）を同一手順として扱う互換判定を追加。
- 表示バージョンを `1.17` から `1.18` へ更新（+0.01）。

## 2026-07-29 12:19 JST
- `書類提出・質問・確認（自宅での作業なし）` の対象で、4項目（黒の手帳へ書いた／カバンへ入れた／提出した／報告した）がすべてチェック済みになった瞬間に宿題・課題本体を自動完了へ更新。
- 判定は既存の `submissionTemplateId` とテンプレートID一致、および `submissionCheckedItemIds` の4項目ID充足で実行。
- 表示バージョンを `1.16` から `1.17` へ更新（+0.01）。

## 2026-07-29 12:14 JST
- `書類提出・質問・確認（自宅での作業なし）` の対象で、提出予定日以降は `提出した` を優先表示し、完了後のみ `報告した` を表示する切り替え処理を追加。
- 提出予定日データ（`item.deadlineDate`）を使って提出日判定を行い、提出日超過後も未完了なら継続表示するよう実装。
- 表示バージョンを `1.15` から `1.16` へ更新（+0.01）。

## 2026-07-29 12:09 JST
- `書類提出・質問・確認（自宅での作業なし）` の対象で、提出予定日の前日以降に `カバンへ入れた` 未完了項目を「未完了の確認」へ表示する処理を追加。
- 提出予定日データ（`deadlineDate`）を使って前日判定を行い、期限超過後も未完了なら継続表示するよう実装。
- 表示バージョンを `1.14` から `1.15` へ更新（+0.01）。

## 2026-07-29 12:04 JST
- `書類提出・質問・確認（自宅での作業なし）` の対象判定から `健康診断書類提出` のタイトル条件を削除。
- `submissionTemplateId` と既存テンプレートIDの関連付けで判定するよう修正し、同テンプレートを設定した全宿題・課題で登録直後の `黒の手帳へ書いた` 未完了表示を有効化。
- 表示バージョンを `1.13` から `1.14` へ更新（+0.01）。

## 2026-07-29 11:59 JST
- `健康診断書類提出`（テンプレート: `書類提出・質問・確認（自宅での作業なし）`）に限り、登録直後の未完了確認で `黒の手帳へ書いた` を表示する処理を追加。
- 既存テンプレートと既存チェック項目IDを利用し、該当項目を完了すると未完了確認から消えるよう実装。
- 表示バージョンを `1.12` から `1.13` へ更新（+0.01）。

## 2026-07-29 10:02 JST
- ホーム画面で「1日の終了」ボタン下に、宿題・課題の締切詳細カード（例: 7/29締切・今日提出）を復元。
- `宿題・課題（〇件）` ボタンは維持したまま、下段の詳細カードを併記する構成に調整。
- 表示バージョンを `1.11` から `1.12` へ更新（+0.01）。

## 2026-07-29 09:52 JST
- スマホでレイアウト変更が反映されない原因を WebView キャッシュ（同一URLの `app.js` / `style.css` 再利用）と特定。
- `index.html` の CSS/JS 読み込みにバージョンクエリを付与し、更新ごとに最新アセットを強制読込するよう修正。
- 表示バージョンを `1.10` から `1.11` へ更新（+0.01）。

## 2026-07-29 09:45 JST
- 出発前チェックの持ち物表示で「提出」由来の項目を除外していたため、ホームに表示される持ち物が出発前で消える不具合を修正。
- 出発前チェックでもホームと同じ持ち物（例: 先生に確認）が表示されるよう反映ロジックを修正。
- 表示バージョンを `1.09` から `1.10` へ更新（+0.01）。

## 2026-07-29 09:44 JST
- 出発前チェックの持ち物参照日付をホーム表示日と同じキーに統一し、ホームで表示される持ち物が出発前チェックにも反映されるよう修正。
- 表示バージョンを `1.08` から `1.09` へ更新（+0.01）。

## 2026-07-29 09:55 JST
- ホーム画面の「宿題・課題」を件数表示カード（`宿題・課題（〇件）`）へ戻し、「タスク実行へ」の直下に配置。
- ホーム上の締切詳細（例: 7/29締切・今日提出）は表示しない状態へ戻し、課題詳細は一覧画面で確認する構成に復元。
- 表示バージョンを `1.07` から `1.08` へ更新（+0.01）。

## 2026-07-29 09:47 JST
- ホーム画面の宿題・課題ボックス配置を調整し、「タスク実行へ」と「1日の終了」の間に表示される元の位置へ復元。
- 表示バージョンを `1.06` から `1.07` へ更新（+0.01）。

## 2026-07-29 09:41 JST
- ホーム画面で「持ち物」を上段カード右側へ移動し、起床・出発・帰宅・勉強の情報と同じカード内で確認できるよう調整。
- 表示バージョンを `1.05` から `1.06` へ更新（+0.01）。

## 2026-07-29 09:33 JST
- ホーム画面の宿題・課題カード位置を調整し、「1日の終了」ボタンの下に表示するよう変更。
- 表示バージョンを `1.04` から `1.05` へ更新（+0.01）。

## 2026-07-29 09:22 JST
- 課題機能を変更前へ戻し、ホーム下段の宿題・課題一覧を常時表示へ復元。
- `今日のアクション`、3状態管理、当日再表示抑止、理由履歴、重複排除の表示と操作を削除し、宿題・課題の完了状態を元の `done` ベースへ戻した。
- 表示バージョンを `1.03` から `1.04` へ更新（+0.01）。

## 2026-07-29 08:55 JST
- 課題の3状態を `done` と `submissionChecklistCompleted` の組み合わせで整合するよう修正し、`done=false && submissionChecklistCompleted=true` を読み込み・保存・操作経路から解消。
- 課題登録・編集画面の文言を `作業の状態` に変更し、`作業中 / 今日のアクションへ送る` で保存するよう整理。
- 表示バージョンを `1.02` から `1.03` へ更新（+0.01）。

## 2026-07-29 08:35 JST
- 課題の状態管理を `作業中 / 作業完成・実行待ち / 最終完了` に整理し、下段一覧・今日のアクション・出発前チェック・帰宅後チェックの表示条件を一本化。
- 帰宅後チェックの「いいえ」は同日中は再表示しないよう抑止し、理由履歴は維持したまま翌日に再表示するよう修正。
- 表示バージョンを `1.01` から `1.02` へ更新（+0.01）。

## 2026-07-29 08:10 JST
- 課題の表示をホーム・出発前チェック・帰宅後チェックで共通化し、ホーム右側を「今日のアクション」に統一。
- 帰宅後チェックで「はい／いいえ」を扱い、「いいえ」の理由を履歴として保存する流れを追加。
- 表示バージョンを `1.00` から `1.01` へ更新（+0.01）。

## 2026-07-29 07:52 JST
- ホーム画面の帰宅後チェック表示条件を見直し、出発前チェック完了後かつ帰宅予定時刻30分前以降のみ表示するよう修正。
- 帰宅前リマインダー、帰宅が遅れそうなら親へ連絡する文言、帰宅後チェックの入力内容と報告文は変更なし。
- 表示バージョンを `0.99` から `1.00` へ更新（+0.01）。

## 2026-07-29 06:37 JST
- 出発前チェックが未完了の間は、ホーム画面に `出発前チェック` ボタンを常時表示するよう修正（時刻帯依存を解除）。
- 出発までの残り時間表示（`あと○分`）は表示可能な時間帯のみ付与し、数分おきの自動案内ロジックは維持。
- 出発前チェックの全体完了条件を見直し、固定チェック項目と持ち物チェック項目の両方が完了した場合のみ `departureCheck.done = true` になるよう変更。
- 持ち物に未チェックが残る場合は、固定項目が完了していても未完了扱いを維持するよう修正。
- 持ち物判定は既存の当日統合持ち物（提出物・定期予定由来・手入力）をそのまま対象にし、`ホームへ戻る` 操作の完了状態変更はなし。
- 表示バージョンを `0.98` から `0.99` へ更新（+0.01）。

## 2026-07-29 06:16 JST
- 入力中判定に `select` を追加し、時刻選択プルダウン操作中も入力中扱いになるよう修正。
- `select` にフォーカスがある間は既存の `requestPassiveRender` 保留ロジックが有効に働き、10秒タイマーや同期起因の再描画を遅延するよう改善。
- `select` のフォーカスが外れた後は既存の `focusout -> flushDeferredUiUpdates` 経路で、保留中の再描画を1回だけ再開する挙動を維持。
- `input` / `textarea` の入力中挙動、画面遷移、保存処理、同期機能、その他機能は変更なし。
- 表示バージョンを `0.97` から `0.98` へ更新（+0.01）。

## 2026-07-29 04:57 JST
- `提出・確認テンプレート` の編集画面でテンプレート名を変更できるよう対応。
- `学校提出物` や `原田先生` のような初期テンプレート名も、そのまま編集可能に変更。
- テンプレートIDは維持し、名前変更後も既存のタスク/宿題の紐づけに影響しないよう保持。
- 表示バージョンを `0.96` から `0.97` へ更新（+0.01）。

## 2026-07-29 04:56 JST
- 設定画面に `出発通知` のタイミング設定を追加し、`なし` から `10分前` までの 12 段階を保存できるよう対応。
- 出発予定時刻を基準にローカル通知を予約するよう変更し、設定変更時は古い通知をキャンセルして新しい設定で再予約するよう修正。
- 通知本文を `予定の出発時刻は○時○分です` に統一し、タスク名は使わないよう変更。
- アプリ再起動時に、保存済みの出発予定時刻と通知設定から通知を復元するよう維持。
- 表示バージョンを `0.95` から `0.96` へ更新（+0.01）。

## 2026-07-29 03:01 JST
- 出発予定時刻の10分前に Android の local notification を予約するよう追加。
- 出発時刻の変更時は古い通知を取消して新しい時刻で再予約し、`外出なし` に切り替えた場合は通知を解除するよう対応。
- アプリ再起動後も、保存済みの出発予定があれば起動時に通知を復元するよう変更。
- タスク終了通知と同じ共通の local notification 予約/取消処理を流用。
- 表示バージョンを `0.94` から `0.95` へ更新（+0.01）。

## 2026-07-29 02:53 JST
- タスク実行中の終了時刻通知を Android の local notification で再予約するよう補強。
- アプリ再起動後でも、保存済みの実行中タスクがあれば起動時に終了通知を復元するよう変更。
- 完了・中断・一時停止中は既存どおり通知を解除し、実行中のみ通知が残るよう維持。
- 表示バージョンを `0.93` から `0.94` へ更新（+0.01）。

## 2026-07-29 02:46 JST
- 帰宅後チェックの表示条件を見直し、`外出前チェック` が一度も起動していない日はホーム通知・時刻リマインドを出さないよう修正。
- `外出前チェック` を手動起動/自動起動した時点で当日フラグを立て、その後にのみ帰宅後チェック導線を表示するよう変更。
- 既存データ互換のため、出発前チェック状態に `activatedOnce` を正規化補完する処理を追加。
- 表示バージョンを `0.92` から `0.93` へ更新（+0.01）。

## 2026-07-29 02:55 JST
- 予定タスクに `targetDateKey` を導入し、通常タスク・定期予定由来タスクを対象日と紐づけて保存するよう修正。
- 予定入力画面の `今日/明日` を実日付・曜日付き表示へ変更（例: `7/29（水） 今日`）。
- 予定入力画面の登録済みタスクを選択中日付で分離表示するよう変更。
- 登録済みタスク欄に `定期予定を一括追加` `定期予定を個別追加` を追加し、個別追加では追加済み判定と重複防止に対応。
- 前日集計・前日終了判定・繰越対象を対象日ベースで絞り込み、翌日予定が前日の未完了へ混入しないよう修正。
- 既存データは読込時に既存所属日の `targetDateKey` を補完する後方互換を追加。
- 表示バージョンを `0.91` から `0.92` へ更新（+0.01）。

## 2026-07-29 02:30 JST
- 設定の `提出・確認テンプレート` 追加方式を、`prompt()` から画面内入力フォーム方式へ変更。
- テンプレート名を入力して `＋ テンプレートを追加` で、任意テンプレートを安定して新規作成できるよう修正。
- 同名テンプレートの重複防止は維持。
- 表示バージョンを `0.90` から `0.91` へ更新（+0.01）。

## 2026-07-29 02:20 JST
- 設定の `提出・確認テンプレート` 一覧に `＋ テンプレートを追加` ボタンを追加。
- `学校提出物` `原田先生` `岩丸先生` 以外の任意テンプレート名を新規作成できるよう対応。
- 同名テンプレートの重複作成を防止。
- 表示バージョンを `0.89` から `0.90` へ更新（+0.01）。

## 2026-07-29 02:05 JST
- ホーム画面から `🚪 ログアウト` を削除し、設定導線のみ残すよう整理。
- ログアウト操作は設定画面側に集約し、ホームヘッダーの見た目を簡素化。
- 表示バージョンを `0.88` から `0.89` へ更新（+0.01）。

## 2026-07-29 01:45 JST
- `提出・確認テンプレート` を予定入力画面から削除し、定期予定の登録・編集画面に移動。
- 定期予定に紐づけたテンプレートを、今日/明日の予定へ自動引き継ぎするよう変更。
- 定期予定から作成したタスクの編集画面ではテンプレート欄と `提出・確認を開く` ボタンを表示しないよう整理。
- 既存の定期予定には既定値 `なし` を付与し、既存の曜日・時間・内容は維持。
- 表示バージョンを `0.87` から `0.88` へ更新（+0.01）。

## 2026-07-29 01:15 JST
- 宿題・課題を新規作成時または未完了から完了へ変更して保存したとき、提出・確認チェック画面の起動対象を保存処理の中で先に確定するよう修正。
- 完了保存直後に `提出・確認` が表示されない分岐漏れを解消し、テンプレート設定済みの宿題・課題で同じチェックフローを再開できるように対応。
- 既にチェック開始済みまたは完了済みの対象は、重複した新規チェックリストを作成しないよう維持。
- 表示バージョンを `0.86` から `0.87` へ更新（+0.01）。

## 2026-07-29 00:45 JST
- `タスク実行` 画面の下部に `未完了の確認` 欄を追加し、未完了の提出・確認チェックリストを対象ごとに表示。
- 表示内容は未チェック項目のみとし、チェック済み項目は省略。
- 各対象の `確認を続ける` から、保存済みチェック状態を保持したまま提出・確認チェック画面を再開可能に変更。
- 全項目確認が完了した対象は `未完了の確認` 欄から自動的に非表示化（チェックリスト完了状態は保存）。
- 1日確定後でも `タスク実行` 画面から未完了チェックの再開を継続できるよう、予定・実績編集ロックと独立して操作可能に維持。
- 表示バージョンを `0.85` から `0.86` へ更新（+0.01）。

## 2026-07-29 00:30 JST
- 設定画面に `提出・確認テンプレート` を追加し、テンプレート項目の追加・名称変更・削除・並び替えに対応。
- 初期テンプレートとして `学校提出物` `原田先生` `岩丸先生` をデフォルト登録。
- タスク登録と宿題・課題登録に `提出・確認テンプレート` 選択（初期値 `なし`）を追加。
- テンプレート設定済みの対象を完了したとき、提出・確認チェック画面を表示し、`後で` で閉じても未完了項目を保持できるよう対応。
- 後から対象（タスク編集/宿題編集）を開いてチェックの続きを再開でき、全項目確認で完了扱いに更新。
- 既存のタスク実行・完了・中断・延長、通知、報告、他画面レイアウトの既存動作は維持。
- 表示バージョンを `0.84` から `0.85` へ更新（+0.01）。

## 2026-07-29 00:05 JST
- 親への予定報告画面で使用する持ち物文言を調整し、`自動持ち物` を `持ち物` に変更。
- 他画面の文言・レイアウト・処理ロジックには変更なし。
- 表示バージョンを `0.83` から `0.84` へ更新（+0.01）。

## 2026-07-28 23:58 JST
- `タスク実行` 画面の初期表示を上位 5 件に固定。
- pending が 6 件以上ある場合、5 件目の下に `すべて見る（全○件）` を表示するよう変更。
- `すべて見る` 押下で全件表示、全件表示中は `折りたたむ` 表示に切り替え、押下で上位 5 件へ戻る挙動を追加。
- タスクの並び順・優先順位判定、実行中/中断中に関する既存表示ルール、実行/完了/中断/延長処理は変更なし。
- ホーム画面のタスク一覧表示件数には変更なし（従来状態を維持）。
- 表示バージョンを `0.82` から `0.83` へ更新（+0.01）。

## 2026-07-28 23:50 JST
- タスク実行画面の上位5件表示について、表示件数を専用定数で管理する実装へ整理。
- 実行候補の抽出を専用ヘルパー化し、pending の既存順序を保持したまま先頭5件のみ表示することを明確化。
- 実行・中断・完了処理、および他画面の表示には影響しないよう、`タスク実行` 画面の描画コードのみに限定して調整。
- 表示バージョンを `0.81` から `0.82` へ更新（+0.01）。

## 2026-07-28 23:35 JST
- タスク上位表示の反映先を修正し、ホーム画面の「5件表示」「すべて見る」変更を撤回。
- ホーム画面のタスク一覧は従来どおり全件表示に戻し、既存の遷移動作を維持。
- `タスク実行` 画面の実行候補リストを 3 件表示から 5 件表示へ変更。
- 6件以上時の扱い、並び順ロジック、実行/中断/完了処理は既存仕様のまま維持。
- 表示バージョンを `0.80` から `0.81` へ更新（+0.01）。

## 2026-07-28 23:20 JST
- ホーム見出しを `今日の予定` から表示中の日付形式（例: `7月28日(火)`）へ変更。
- 1日の終了でコピー完了後、ホームへ戻して翌日表示モードに切り替えるよう調整。
- ホームに `＜ 前日を見る` と `翌日の予定へ戻る` を追加し、前日表示中は実行・予定入力・宿題・1日の終了を編集不可（閲覧専用）に変更。
- 前日表示用に終了日の予定・タスク・持ち物をアーカイブし、切替表示時に参照するよう更新。
- 表示バージョンを `0.79` から `0.80` へ更新（+0.01）。

## 2026-07-28 23:00 JST
- タスク完了後に表示される `保護者への報告文` ページから、報告文のコピー導線を削除。
- `親への予定報告` ページから `実績+次の日予定をメール作成` ボタンを削除。
- 予定報告のコピーは残し、報告文ページでは確認表示のみとするよう調整。
- 表示バージョンを `0.78` から `0.79` へ更新（+0.01）。

## 2026-07-28 22:40 JST
- ホーム画面のタスク一覧を 3 件表示から 5 件表示へ変更。
- 6 件以上ある場合は、5 件目の下に `すべて見る（全○件）` を表示し、押下で全タスクを表示するよう更新。
- 実行中または中断中のタスクは一覧の先頭に寄せて表示するよう調整。
- タスク実行処理やタスク実行より下のレイアウトは変更せず、ホーム一覧の表示ロジックのみ変更。
- 表示バージョンを `0.77` から `0.78` へ更新（+0.01）。

## 2026-07-28 22:36 JST
- `同期：...` の文言をヘッダーから削除し、ホームカードの最下部へ移動。
- ヘッダー右側の同期表示枠は空にし、カード末尾に `同期：...` を表示するよう変更。
- ホーム画面のその他レイアウト、文字サイズ、ログアウト・設定の配置は変更なし。
- 表示バージョンを `0.76` から `0.77` へ更新（+0.01）。

## 2026-07-28 22:27 JST
- スマホ版の `Self Support PJR` を `今日の予定` と同じ大きさ・太字で維持するよう調整。
- `version 0.75` は小さいまま残し、アプリ名とバージョンの見た目を分離。

## 2026-07-28 22:33 JST
- スマホ版で上に寄せすぎて消えていた `設定` ボタンを戻し、ヘッダー右側の位置を下げて再表示。
- `Self Support PJR` と `ログアウト` の高さ差を縮めるよう、右側の上寄せを弱めて調整。
- 既存の文字サイズ設定は維持しつつ、モバイルでの視認性を優先して位置のみ再調整。
- 表示バージョンを `0.75` から `0.76` へ更新（+0.01）。

## 2026-07-28 22:21 JST
- ヘッダーの `Self Support PJR` を `今日の予定` と同じフォントサイズ・太字へ変更。
- `version 0.72` は今の小さいサイズのまま残し、アプリ名とバージョンを別スパンで分離表示。
- 既存のヘッダー余白・日付配置・同期表示・ホームカードのレイアウトは変更なし。
- 表示バージョンを `0.72` から `0.73` へ更新（+0.01）。
## 2026-07-28 22:17 JST
- ヘッダーのタイトル `今日の予定を立てる` を削除。
- ホーム上部の余白を詰めるため、ヘッダー上下パディングを縮小。
- `今日の予定` カードの内側余白と上下マージンを縮小し、カード周辺スペースをコンパクト化。
- `今日の予定` 見出し行の下余白も縮小し、見出しとカードの間隔を短縮。
- 表示バージョンを `0.71` から `0.72` へ更新（+0.01）。

## 2026-07-28 22:03 JST
- スマホ幅で 今日の予定 カードが1カラム化され、持ち物が時刻行の下へ落ちる表示を修正。
- スマホ表示でも2カラムを維持するよう変更し、持ち物を起床/出発/帰宅/勉強の右側に表示。
- スマホ時のカラム間隔を調整し、カードのコンパクトさを維持。
- 表示バージョンを 0.70 から 0.71 へ更新（+0.01）。

## 2026-07-28 21:52 JST
- ホーム画面の 予定入力 ラベル文字を太字へ明示調整。
- 今日の予定 と同じ太さになるよう、フォントウェイトを 800 に固定。
- 既存のサイズ、位置、透過オレンジ背景、遷移動作は変更なし。
- 表示バージョンを 0.69 から 0.70 へ更新（+0.01）。

## 2026-07-28 21:50 JST
- ホーム画面の `予定入力` ラベルの文字サイズ・太さ・行高を `今日の予定` と同一に明示設定し、見出しサイズを一致。
- `予定入力` の背景色を濃いオレンジから、透過感の高いオレンジへ変更。
- 枠線・影も透過度を上げ、軽い見た目を維持しつつボタンらしさは保持。
- ホバー/フォーカス時も濃色ではなく、透過オレンジの濃度アップで表現するよう調整。
- 表示バージョンを `0.68` から `0.69` へ更新（+0.01）。

## 2026-07-28 21:49 JST
- ホーム画面の `予定入力` ボタン風ラベルについて、文字色を白から黒へ変更。
- オレンジ背景・枠線・角丸・影・左1cmオフセットなど、既存のボタン風スタイルは維持。
- ホバー/フォーカス時の文字色も黒へ統一。
- 表示バージョンを `0.67` から `0.68` へ更新（+0.01）。

## 2026-07-28 21:48 JST
- ホーム画面の `予定入力` 文字を左へ約1cm移動。
- `予定入力` を押せる印象にするため、オレンジ背景・角丸・枠線・影を追加してボタン風デザインへ変更。
- ホバー/フォーカス時はオレンジを濃くして押下可能な視覚フィードバックを強化。
- 既存の遷移動作（予定入力画面へ移動）は変更なし。
- 表示バージョンを `0.66` から `0.67` へ更新（+0.01）。

## 2026-07-28 21:43 JST
- ホームヘッダーの日付表示を `本日：...` に戻し、`今日の予定を立てる` と `今日の予定` の間へ配置。
- 同期表示は右側へ戻し、`同期：...` として表示。
- 日付サイズは変更せず、`今日の予定を立てる` と日付の縦間隔のみ詰めて表示。
- `同期 / 設定 / ログアウト` の上寄せ配置（約2cm）は維持。
- 表示バージョンを `0.65` から `0.66` へ更新（+0.01）。

## 2026-07-28 21:40 JST
- ホームヘッダーの表示位置を調整し、`本日：日付` の位置に `同期：...` を表示するよう変更。
- 日付表示は右側へ移動し、`同期 / 設定 / ログアウト` を全体的に約2cm上へオフセットして配置。
- 操作導線（設定遷移・ログアウト動作）と他画面のナビ挙動は変更なし。
- 表示バージョンを `0.64` から `0.65` へ更新（+0.01）。

## 2026-07-28 21:38 JST
- ホーム画面の `設定` / `ログアウト` を、白カード内上部ではなくヘッダー右側へ移動。
- `同期：...` 表示の上部スペースに `⚙ 設定` / `🚪 ログアウト` を縦2行で配置。
- ホーム画面では白カード内のトップナビを非表示化し、非ホーム画面の `ホーム/戻る` ナビは従来どおり維持。
- 表示バージョンを `0.63` から `0.64` へ更新（+0.01）。

## 2026-07-28 21:31 JST
- ホーム画面レイアウトを調整し、変更対象をヘッダーと「今日の予定」エリアのみに限定して更新。
- ヘッダー右上に文字表示の `⚙ 設定` / `🚪 ログアウト` を縦2行で配置し、ホームカード内の設定・ログアウト導線は削除。
- 日付表示を `本日：...` に統一し、同一行右側へ `同期：...` を表示。
- 「今日の予定」見出し右側に、同一サイズの文字導線 `予定入力` を配置（ボタン化せず、タップで従来どおり予定入力へ遷移）。
- 「今日の予定」カードを2カラム化し、左に `起床/出発/帰宅/勉強`、右に持ち物エリア（未登録時 `持ち物：なし`、登録時は改行リスト）を表示。
- カード内余白・行間を詰め、区切り線なしで全体の高さをコンパクト化。
- `タスク実行へ` ボタンより下のレイアウト・機能・データ構造は変更なし（`予定入力へ` のみ指定どおり見出し側へ移動）。
- 表示バージョンを `0.62` から `0.63` へ更新（+0.01）。

## 2026-07-28 19:34 JST
- ホーム画面の帰宅後チェック通知をさらに強調し、枠線を赤色の太線（4px）へ変更。
- 通知背景を目立つクリーム系グラデーションへ変更し、視認性を向上。
- 通知内ボタンの文字色・枠線も赤系へ合わせ、カード全体で一貫して目立つよう調整。
- 表示バージョンを `0.61` から `0.62` へ更新（+0.01）。

## 2026-07-28 19:32 JST
- 左のホーム画面通知文言を、`帰宅時チェックが未完了です。` から `帰宅後チェックが未完了です。` へ調整。
- ホーム通知ボタン文言も `帰宅後チェックをする` に合わせて更新。
- 前回追加した強調配色（背景・文字・枠線・影）は維持し、見た目の目立ちやすさはそのまま。
- 表示バージョンを `0.60` から `0.61` へ更新（+0.01）。

## 2026-07-28 19:29 JST
- ホーム画面左側の `帰宅時チェックが未完了です。` 通知を目立つ配色へ変更。
- 該当通知に専用クラス `return-check-notice` を追加し、背景を強めのピンク系グラデーション、文字色を濃色、境界線と影を追加して視認性を強化。
- 通知内ボタンも白背景＋濃色文字＋枠線に調整し、通知カード上での可読性と操作導線を改善。
- 出発前チェックなど他の `notice warn` 表示には影響しないよう、帰宅時チェック通知のみに限定して反映。
- 表示バージョンを `0.59` から `0.60` へ更新（+0.01）。

## 2026-07-28 19:28 JST
- 画面上の「帰宅後チェック」表記を「帰宅時チェック」へ統一して表示。
- ホーム通知、遷移ボタン、リマインダーダイアログ、帰宅時チェック画面見出し、コピー文面などの表示文言を更新。
- クリーンアップ確認ダイアログの文言も `出発前/帰宅時チェック` に合わせて調整。
- 既存の帰宅時チェック判定ロジック・通知タイミング・保存処理は変更なし。
- 表示バージョンを `0.58` から `0.59` へ更新（+0.01）。

## 2026-07-28 19:00 JST
- ホーム画面の `帰宅後チェックが未完了です。` 通知を、出発前チェックと同じオレンジ系の強調表示へ変更。
- 表示クラスを `notice info` から `notice warn` に変更し、目立つ色合いを統一。
- 通知表示条件・ボタン遷移（`帰宅後チェックをする`）など挙動ロジックは変更なし。
- 表示バージョンを `0.57` から `0.58` へ更新（+0.01）。

## 2026-07-28 18:46 JST
- 実機でホーム画面の `帰宅後チェックが未完了です。` が表示されない原因を修正。
- 原因は、`scripts/sync-web-assets.ps1` が `www/` のみ更新し、実機が参照する `android/app/src/main/assets/public/` が古いままだったこと。
- 同期スクリプトを更新し、`index.html` / `app.js` / `style.css` を `www/` と `android/app/src/main/assets/public/` の両方へ同時コピーするよう変更。
- ソース未存在時の削除処理も両方のコピー先に適用。
- 表示バージョンを `0.56` から `0.57` へ更新（+0.01）。

## 2026-07-28 18:44 JST
- 帰宅後チェックの「帰宅予定時刻が近づいています」ポップアップで `あとで` 押下後、3分間は再表示しないクールダウンを追加。
- 3分経過後は、帰宅後チェック未完了かつ表示条件を満たす場合に既存仕様どおり再表示。
- 既存の30分前表示、予定時刻超過後の文言切替、`帰宅後チェックへ` 遷移は維持。
- 帰宅後チェック完了時・`帰宅後チェックへ` 遷移時にはクールダウン状態をクリアするよう調整。
- 表示バージョンを `0.55` から `0.56` へ更新（+0.01）。

## 2026-07-28 18:39 JST
- ホーム画面「今日の予定」の持ち物表示を左右レイアウトへ変更し、`持ち物` ラベルは左・内容（`なし` 含む）は右側に表示するよう調整。
- 持ち物の値は右寄せし、長い文字列でも折り返して表示できるように調整。
- 既存の持ち物判定ロジック・他機能の挙動は変更なし。
- 表示バージョンを `0.54` から `0.55` へ更新（+0.01）。

## 2026-07-28 18:29 JST
- ホーム画面「今日の予定」の持ち物表示文言を再調整し、持ち物がない場合に `持ち物：なし` と明示表示されるよう修正。
- 持ち物がある場合も `持ち物：...` の同一フォーマットで表示するよう統一。
- 持ち物判定ロジックや他機能の挙動は変更なし。
- 表示バージョンを `0.53` から `0.54` へ更新（+0.01）。

## 2026-07-28 18:18 JST
- 帰宅後チェックのポップアップで `あとで` を押して閉じた後でも導線が残るよう、ホーム画面上部に `帰宅後チェックをする` ボタンを追加。
- 帰宅後チェックが未完了の間はホーム上部にボタンを表示し、押下で帰宅後チェック画面へ遷移する仕様を追加。
- 帰宅後チェック完了後は既存の完了フラグに連動してボタンを非表示化。
- 既存ポップアップ（30分前表示・予定時刻超過時文言切替・`あとで` 動作）は変更なし。
- 出発前チェックの表示条件・処理、その他機能は変更なし。
- 表示バージョンを `0.52` から `0.53` へ更新（+0.01）。

## 2026-07-28 18:17 JST
- ホーム画面の「今日の予定」概要に、持ち物の有無に関係なく `持ち物` 行を常時表示するよう更新。
- 持ち物がある場合は一覧を表示し、ない場合は `なし` と表示する仕様に変更。
- 予定・タスク・通知・チェック系の既存処理は変更せず、表示のみ調整。
- 表示バージョンを `0.51` から `0.52` へ更新（+0.01）。

## 2026-07-28 18:01 JST
- タスク実行画面の通常操作ボタン `完了` / `中断` を、スマホ幅でも横一列（2列均等）で表示するよう調整。
- 2ボタンは同幅で画面幅いっぱいに均等配置し、縦並びへ崩れないよう専用レイアウトクラスを追加。
- ボタン高さ・既存の完了/中断処理ロジック・他機能は変更なし。
- 表示バージョンを `0.50` から `0.51` へ更新（+0.01）。

## 2026-07-28 17:59 JST
- スマホ前提の延長確認表示へ調整し、「予定時間を超えました。」直下で `10分延長` / `20分延長` を横並び表示するよう更新。
- `10分延長` または `20分延長` 押下時は、同じ位置の行を `キャンセル` / `延長する` の2ボタン表示に切替える仕様へ修正。
- `延長する` で選択済み分数をそのまま延長し、`キャンセル` で `10分延長` / `20分延長` 表示へ復帰する既存遷移を維持。
- 時間指定延長はスマホでも `［分数入力欄］［キャンセル］［延長する］`（通常時は `［分数入力欄］［延長する］`）を横一列維持するよう調整。
- スマホ幅で画面内に収まるよう、延長エリアのボタン幅と入力欄幅を調整（高さ・可読性は維持）。
- 延長処理・確認処理・入力値保持以外の挙動、他画面機能は変更なし。
- 表示バージョンを `0.49` から `0.50` へ更新（+0.01）。

## 2026-07-28 17:48 JST
- タスク実行画面の開発用機能 `1分テスト設定` を削除。
- `1分テスト設定` ボタン（UI）と、1分化のための専用クリック処理を削除。
- 通常の予定・タスク・通知・延長・確認・入力保持ロジックには影響しないよう、開発用コードのみを対象に変更。
- その他の機能およびレイアウトは変更なし。
- 表示バージョンを `0.48` から `0.49` へ更新（+0.01）。

## 2026-07-28 17:37 JST
- タスク実行画面の「時間指定延長（分）」確認表示をスマホ幅（`max-width: 600px`）で2段表示へ調整。
- 1段目に入力欄（100%幅）、2段目に `キャンセル` / `延長する` を横並び・同幅で表示し、画面外へはみ出さないように更新。
- PC幅では既存の横一列レイアウト（入力欄＋ボタン群）を維持し、表示のみを切り替えるレスポンシブ対応を追加。
- 延長処理・確認処理・入力値保持の既存ロジックは変更なし。
- 表示バージョンを `0.47` から `0.48` へ更新（+0.01）。

## 2026-07-28 04:12 JST
- 帰宅予定時刻の30分前から、画面に依存せず表示できる「帰宅後チェック」リマインダーポップアップを追加（`あとで` / `帰宅後チェックへ`）。
- `あとで` 選択時は現在画面を維持したまま閉じ、帰宅後チェック完了までホームへ戻るたびに再表示する仕様を追加。
- 帰宅予定時刻を過ぎて未完了の場合は、ポップアップ文言を「帰宅後チェックが未完了です。帰宅が遅れそうな場合は、保護者へ連絡してください。」へ自動切替。
- 帰宅後チェック項目に「帰宅が遅れそうな場合は、親へ連絡しましたか？」を追加し、回答を `はい / いいえ / 該当なし` の3択で保存・コピー・報告文へ反映。
- 帰宅後チェック完了後は当日のポップアップを再表示しないように調整。
- LINEを開く既存機能・自動送信・時間指定スヌーズ・既存項目・他画面ロジックは変更なし。
- 表示バージョンを `0.46` から `0.47` へ更新（+0.01）。

## 2026-07-28 03:52 JST
- 時間指定延長ブロックを再修正し、通常時は `［分数入力欄］［延長する］`、確認時は `［分数入力欄］［キャンセル］［延長する］` を必ず横一列で表示する構造へ変更。
- 時間指定延長専用ラッパー `.custom-extend-control` を使用し、`display: flex` / `flex-direction: row` / `align-items: stretch` / `flex-wrap: nowrap` を適用。
- 入力欄 `#extendCustom` は `flex: 1` にし、ボタンは `flex: 0 0 auto` で必要幅のみ確保。縦積みにならないよう調整。
- 親の専用領域 `.custom-extend-field` は通常幅で 50%・左寄せ、狭幅画面のみ 100% へ広げるレスポンシブ調整を追加。
- 延長処理/確認処理のロジックは変更せず、表示レイアウトのみ更新。
- 表示バージョンを `0.45` から `0.46` へ更新（+0.01）。

## 2026-07-28 03:41 JST
- タスク実行画面の「時間指定延長（分）」入力＋「延長する」一体コントロールの親コンテナ幅を、現状比で約50%に調整（左寄せのまま）。
- 内部の入力欄とボタンの比率は固定値幅から比率指定へ変更し、約 70 : 30 を維持するよう更新。
- 確認表示（キャンセル / 延長する）も同一コンテナ内で幅比率に追従するよう調整。
- デザイン（色・フォント・余白トーン）や延長ロジックは変更せず、横幅制御のみ変更。
- 表示バージョンを `0.44` から `0.45` へ更新（+0.01）。

## 2026-07-28 03:31 JST
- タスク実行画面の「時間指定延長（分）」を、添付イメージに合わせて入力欄とアクションボタンの横並びコンパクトレイアウトへ変更。
- 白い入力枠の横幅をさらに短くし、右側の `延長する`（確認時は `キャンセル / 延長する`）と一体感のある見た目に調整。
- `10分延長` / `20分延長` の確認切替ロジックや既存の延長処理は変更せず、時間指定延長ブロックの表示構造のみ更新。
- 表示バージョンを `0.43` から `0.44` へ更新（+0.01）。

## 2026-07-28 03:22 JST
- タスク実行画面の「時間指定延長（分）」入力欄（白枠）がまだ長く見えるため、横幅上限をさらに縮小してコンパクト化。
- `#extendCustom` の幅を `min(170px, 100%)` から `min(110px, 100%)` へ変更し、延長確認ボタン群との視覚バランスを調整。
- 表示バージョンを `0.42` から `0.43` へ更新（+0.01）。

## 2026-07-28 03:14 JST
- タスク実行画面の「時間指定延長（分）」入力欄の横幅を縮小し、確認時の `キャンセル` ボタンと同程度のサイズ感になるよう調整。
- 入力欄ID `#extendCustom` に幅上限を設定し、既存の他入力欄レイアウトや延長処理ロジックには影響しないよう変更。
- 表示バージョンを `0.41` から `0.42` へ更新（+0.01）。

## 2026-07-28 03:00 JST
- タスク実行画面の確認方法を変更し、`完了` 押下時は同じ位置で `キャンセル / 完了する` に切替える方式へ更新（最下部の確認表示は廃止）。
- 予定超過時の延長操作（10分/20分/時間指定）も同様に、押したボタン位置で `キャンセル / 延長する` へ切替える方式へ更新。
- `延長する` は押下時に選択または入力済みの延長分でそのまま既存の延長処理を実行し、処理内容自体は変更なし。
- ボタン位置は維持し、確認方法のみを変更。既存の完了・延長・中断・保存挙動には影響を与えないよう調整。
- 表示バージョンを `0.40` から `0.41` へ更新（+0.01）。

## 2026-07-28 02:49 JST
- 実績報告文の送信前確認を追加し、予定時間の2倍以上かつ予定より30分以上長いタスクが1件でもある場合は確認メッセージを表示するよう変更。
- 条件判定は各タスクの `plannedMinutes` と `actualSeconds` から算出し、例示どおり `30→60` は表示、`20→40` は非表示となるよう実装。
- 条件に該当する場合のみ、結果報告画面と1日の終了画面のコピー前に確認ダイアログを表示。条件外では従来どおりそのままコピーできる仕様を維持。
- 既存の送信文面、実績修正機能、画面デザイン、その他の動作は変更なし。
- 表示バージョンを `0.39` から `0.40` へ更新（+0.01）。

## 2026-07-28 02:44 JST
- 予定入力画面の「今日」「明日」の表示順を `今日` → `明日` に変更。
- 選択値、保存先、既存データ、予定入力/編集の動作は変更せず、並び順のみを調整。
- 表示バージョンを `0.38` から `0.39` へ更新（+0.01）。

## 2026-07-28 02:41 JST
- 宿題・課題の提出日表示に曜日を追加し、`8/1（金）締切` の形式で表示するよう更新。
- ホーム画面の宿題要約欄と宿題・課題一覧の両方で同じ曜日表示を適用。
- 仕様や動作は変更せず、表示のみを調整。
- 表示バージョンを `0.37` から `0.38` へ更新（+0.01）。

## 2026-07-28 02:38 JST
- 宿題・課題の残り日数表示を、締切日とは別要素に分離し、`あと4日` などの残り日数だけを強調色と少し大きい文字で表示するよう調整。
- ホーム画面の宿題要約欄と宿題・課題一覧の両方で同じ強調表示を適用。
- 仕様や動作は変更せず、表示のみを調整。
- 表示バージョンを `0.36` から `0.37` へ更新（+0.01）。

## 2026-07-28 02:33 JST
- 宿題・課題の提出期限表示を追加し、JST基準の暦日差で `提出まであと○日` / `今日提出` / `提出期限超過` を計算するヘルパーを実装。
- ホーム画面の「宿題・課題」欄に、未完了の宿題・課題ごとの提出日と残り日数を表示する要約欄を追加。
- 宿題・課題一覧画面の既存締切表示を、`8/4締切　提出まであと7日` のように提出日と残り日数の併記へ更新。
- 完了済みの宿題・課題は従来どおり一覧対象外のため、残り日数表示も未完了のみ。
- 表示バージョンを `0.35` から `0.36` へ更新（+0.01）。

## 2026-07-28 02:27 JST
- ホーム画面の中断中 `【再開】` 表示色を、より明るい赤へ変更。
- 点滅アニメーション周期を遅くし、ゆっくり点滅するよう調整。
- 仕様や動作は変更せず、表示のみを調整。
- 表示バージョンを `0.34` から `0.35` へ更新（+0.01）。

## 2026-07-28 02:24 JST
- ホーム画面の中断中 `【再開】` 表示をさらに濃い赤へ変更し、点滅アニメーションを追加して視認性を強化。
- `prefers-reduced-motion` 環境では点滅を無効化し、表示負荷を抑えるよう調整。
- 仕様や動作は変更せず、表示のみを調整。
- 表示バージョンを `0.33` から `0.34` へ更新（+0.01）。

## 2026-07-28 02:18 JST
- ホーム画面の中断中タスク表示を整理し、一覧の `【再開】` に赤字・太字の強調表示を追加。
- 画面下部の重複メッセージ `中断中タスクがあります。再開してください。` を削除し、ホーム画面の表示を簡潔化。
- 仕様や動作は変更せず、表示のみを調整。
- 表示バージョンを `0.32` から `0.33` へ更新（+0.01）。

## 2026-07-28 02:10 JST
- 予定入力、定期予定、宿題・課題の各保存処理を保護し、保存ボタン押下時に最新の入力値をDOMから即時反映してから保存するよう変更。入力直後の1回目タップが `focusout` / 再描画に奪われる経路を抑止。
- 保存中は対象の「追加」「保存」を `保存中...` 表示にし、戻る・ホーム・設定・行操作など競合しうる操作を一時無効化。保存完了前の画面遷移/再描画を抑制。
- 保存成功時は一時メッセージを表示し、予定は `予定を保存しました`、定期予定は `定期予定を登録しました` を表示。失敗時は入力内容を復元したままエラーメッセージを表示。
- Firestore同期の保存キューを修正し、保存中に新しい保存が入っても最新ハッシュを再送するよう変更。保存直後に古いリモート状態で上書きされるリスクと、同期中の保存取りこぼしを低減。
- 表示バージョンを `0.31` から `0.32` へ更新（+0.01）。

## 2026-07-28 01:21 JST
- タスク開始/再開時に、`alertAtSeconds` と経過秒数から終了予定時刻を計算し、Capacitor Local Notifications を終了時刻へ予約する実装を追加。
- 通知予約処理を共通関数へ切り出し、通知テストボタンの既存機能（10秒後通知）は共通関数経由へ置き換えて挙動を維持。
- タスク終了通知のIDをタスク単位で安定生成し、同じタスクの再開始/再開時に古い通知を先にキャンセルしてから再予約するよう変更。
- タスクを予定前に中断/完了/今日は終了した場合、該当タスクの予約済み終了通知をキャンセルするよう変更。
- 既存のWeb側アラート音・バイブ処理、手動完了フロー、画面/保存仕様は変更なし。
- 表示バージョンを `0.30` から `0.31` へ更新（+0.01）。

## 2026-07-27 20:42 JST
- Capacitor依存のバージョン指定を固定化し、`package.json` の `@capacitor/android` / `@capacitor/core` / `@capacitor/local-notifications` / `@capacitor/cli` を v8系の整合する実バージョンへ更新（`@capacitor/cli` は `devDependencies` へ移動）。
- `index.html` から `capacitor.js` の手動scriptタグを削除し、表示バージョンを `0.29` から `0.30` へ更新（+0.01）。
- Local Notifications 呼び出しを、非推奨の `Plugins` 直参照依存を減らすため `registerPlugin("LocalNotifications")` 優先へ更新し、通知チャンネル定義から不適切な `sound: "default"` を除去。
- `scripts/sync-web-assets.ps1` を改善し、同期元に存在しない対象ファイルが `www` に残存しないよう安全な削除処理を追加（staleファイル混入を抑止）。
- 実行確認: `npm install` 成功、`npm run sync:web` 成功、`npx cap add android` 成功（`android/` 生成）、`npx cap sync android` 成功（Local Notifications プラグイン検出・同期完了）。

## 2026-07-27 10:40 JST
- SelfSupportApp を Capacitor Android 試作版へ拡張するため、`package.json`、`capacitor.config.json`、`scripts/sync-web-assets.ps1` を追加し、静的HTML/CSS/JSを `www` へ同期して利用する構成を追加。
- 設定画面に独立した試験用「10秒後に通知」ボタンを追加し、Capacitor Local Notifications 経由で通知権限確認、Android 8+ 通知チャンネル作成、10秒後のローカル通知予約を行う試験コードを追加。
- 通知内容は「タスク終了 / 数学の終了時間です」とし、通知音・振動付きチャンネルを利用する実装を追加。Webブラウザ上では非対応メッセージのみ表示。
- この環境では Node.js / npm 未導入のため、依存インストール、`npx cap add android`、`android/` 生成、実機ビルドは未実行。
- 表示バージョンを `0.28` から `0.29` へ更新（+0.01）。

## 2026-07-27 10:36 JST
- 予定入力の定期予定適用で、`recurringPlansAppliedByDate` が true のままでもタスクが0件の場合は stale フラグとみなして当日フラグを解除し、1回だけ再適用できる回復処理を追加。
- これにより、同期で古い適用済みフラグが戻ってしまった場合でも、今日/明日切替で定期予定が全く出ない状態から復帰可能に。
- タスクが1件以上ある通常状態では従来どおり適用済みなら再追加しない挙動を維持。
- 表示バージョンを `0.27` から `0.28` へ更新（+0.01）。

## 2026-07-27 10:32 JST
- 「当日の状態をクリーンにする」実行時に `recurringPlansAppliedByDate` を初期化するよう修正。
- これにより、強制クリア後に今日/明日へ切り替えた際、定期予定が適用済み扱いでブロックされて表示されない問題を解消。
- 既存の宿題・課題保持やその他クリーン対象の挙動は変更なし。
- 表示バージョンを `0.26` から `0.27` へ更新（+0.01）。

## 2026-07-27 10:27 JST
- 出発前チェックの進捗表示整合性を修正し、`remainingIndices`（未確認キュー）に残っている項目は `completedIndices`（済み）から除外する正規化を追加。
- これにより、未確認なのに下段「確認状況」が「済」と表示される矛盾を防止し、表示と確認項目の一致を維持。
- 表示バージョンを `0.25` から `0.26` へ更新（+0.01）。

## 2026-07-27 10:26 JST
- 出発前チェックの「確認した」「まだ」処理を堅牢化し、常にキュー先頭項目を対象に進行/回転するよう変更。
- 描画タイミング差で項目インデックスが不一致になった場合でも no-op にならず、`確認した` で確実に進むよう修正。
- キュー先頭が不正な場合は完了扱いへフォールバックし、画面が残り続ける状態を回避。
- 表示バージョンを `0.24` から `0.25` へ更新（+0.01）。

## 2026-07-27 10:22 JST
- 予定入力の定期予定適用ロジックを強化し、日付フラグ方式に加えて「同一日付 × 同一定期予定ID」の重複追加を防止するガードを追加。
- 状態読込時に、既存タスクの `recurringDateKey` から日付適用フラグを補完する処理を追加し、同期や旧データ読込でフラグが欠けても再増殖しにくいよう改善。
- これにより、今日/明日切替で同一定期予定が重複追加される再発リスクを低減。
- 表示バージョンを `0.23` から `0.24` へ更新（+0.01）。

## 2026-07-27 10:14 JST
- 出発前チェックの状態正規化を修正し、`remainingIndices` が空配列の場合でも進捗状態（完了済み/完了フラグ）に応じて正しく保持するよう変更。
- 初回未初期化状態（空配列・完了履歴なし・done=false）のみ全項目キューを初期化し、それ以外の空配列は巻き戻さないよう調整。
- これにより「確認した」押下後にチェック項目が消えず進まない/巻き戻る問題を解消。
- 表示バージョンを `0.22` から `0.23` へ更新（+0.01）。

## 2026-07-27 10:12 JST
- 「予定を作る日（今日/明日）」の change 処理を修正し、`checked` になったラジオのみ処理するよう変更。
- 未選択側ラジオの change 発火では処理しないため、日付切替1回で定期予定適用処理が複数回走る可能性を除去。
- あわせて、選択値が既に同じ場合は処理しないガードを追加し、不要な再適用・再保存を防止。
- 表示バージョンを `0.21` から `0.22` へ更新（+0.01）。

## 2026-07-27 10:07 JST
- 定期予定の自動追加方式を変更し、日付ごとの適用済みフラグ `recurringPlansAppliedByDate` を state に追加。
- 「今日/明日」の選択時のみ、対象日付が未適用なら定期予定を1回だけ追加し、追加完了後に当日日付フラグを `true` で保存するよう変更。
- `render()` から定期予定追加処理の呼び出しを削除し、画面再描画・10秒タイマー・Firebase同期受信では自動追加が走らないよう修正。
- 既存データ互換として、従来の `recurringSyncDateKey` がある場合は `recurringPlansAppliedByDate` へ反映する正規化を追加。
- 表示バージョンを `0.20` から `0.21` へ更新（+0.01）。

## 2026-07-27 09:54 JST
- 「予定入力」画面の定期予定自動追加を修正し、同一日付に対する同一定期予定の再追加を防止。
- 定期予定から生成したタスクに、元の定期予定ID（`recurringPlanId`）と対象日付（`recurringDateKey`）を保持するよう変更。
- 自動追加時は「同一日付 × 同一定期予定ID」の既存タスク有無を判定し、存在する場合は追加しないよう変更（予定名では判定しない）。
- 既存の手動追加タスク・編集済みタスク・画面遷移・同期処理は変更なし。
- 表示バージョンを `0.19` から `0.20` へ更新（+0.01）。

## 2026-07-27 01:38 JST
- スマホ入力不具合の最小修正として、10秒ごとの画面更新処理で `render()` 直接呼び出しを `requestPassiveRender()` に変更し、IME入力中/編集中の強制再描画を抑制。
- 日本語入力を扱う主要な `input` ハンドラ（宿題・課題、定期予定、予定入力、帰宅後チェック、レビュー自由メモ）に IME変換中ガード（`e.isComposing || isComposingText`）を追加。
- `compositionend` 後に確定文字列の `input` を再送する処理を追加し、変換確定後の state 保存漏れを防止。
- 表示バージョンを `0.18` から `0.19` へ更新（+0.01）。

## 2026-07-27 01:21 JST
- 「当日の状態をクリーンにする」の確認文言を更新し、宿題・課題（締切管理）は削除対象外で保持されることを明記。
- 今日の予定タスク削除と、宿題・課題データ保持の区別が画面上で分かるよう調整。
- 表示バージョンを `0.17` から `0.18` へ更新（+0.01）。

## 2026-07-27 01:20 JST
- 「当日の状態をクリーンにする」実行時に時刻が残る問題を修正し、起床・出発・帰宅・勉強開始時刻を空状態へクリアするよう変更。
- これにより、クリーン後のホーム「今日の予定」で時刻が残らず `--:--` 表示になるよう調整。
- 表示バージョンを `0.16` から `0.17` へ更新（+0.01）。

## 2026-07-27 01:19 JST
- 「当日の状態をクリーンにする」の挙動を変更し、ステータス初期化だけでなく今日の予定タスク自体を削除するよう更新。
- あわせて当日の時刻設定、今日だけ追加持ち物、実行中/完了状態、出発前チェック・帰宅後チェックを当日初期状態へ戻すよう調整。
- 確認ダイアログ文言を、削除を含む挙動が分かる内容へ変更。
- 表示バージョンを `0.15` から `0.16` へ更新（+0.01）。

## 2026-07-27 01:17 JST
- 予定時間超過時の既存1回目アラート（約5秒）は維持したまま、30秒後の2回目強アラート（約10秒）を追加。
- 2回目アラートは同一タスク・同一しきい値で多重予約されないよう制御を追加。
- 「通知停止」「完了」「今日は終了」「10分延長」「20分延長」「任意延長」操作時に、2回目アラート予約を解除し、再生中なら即停止するよう対応。
- 予定時間判定、タイマー進行、画面遷移、既存ボタン本来動作、バイブ処理は変更なし。
- 表示バージョンを `0.14` から `0.15` へ更新（+0.01）。

## 2026-07-27 01:14 JST
- 予定確認画面の持ち物表示を強調し、赤色かつ太字で表示されるようスタイルを更新。
- 表示バージョンを `0.13` から `0.14` へ更新（+0.01）。

## 2026-07-27 01:13 JST
- 親への予定報告画面の左上ボタンを「ホーム」から「戻る」へ変更し、押下時はひとつ前の画面へ戻るよう修正。
- 他画面の左上ボタン挙動（ホーム/ログアウト）は従来どおり維持。
- 表示バージョンを `0.12` から `0.13` へ更新（+0.01）。

## 2026-07-27 01:11 JST
- 親への予定報告（予定コピー本文）に持ち物行を追加し、自動連係持ち物と今日だけ追加持ち物を表示するよう変更。
- 持ち物が存在する場合のみ表示する仕様とし、不要な「なし」表示は出さないよう調整。
- 表示バージョンを `0.11` から `0.12` へ更新（+0.01）。

## 2026-07-27 01:10 JST
- 「明日の予定を確認してください」画面の出発時刻右側表示を調整し、今日だけ追加した持ち物も表示されるよう変更。
- 自動持ち物は、連係対象がない場合は表示しない仕様に変更（"自動持ち物: なし" を非表示化）。
- 表示バージョンを `0.10` から `0.11` へ更新（+0.01）。

## 2026-07-27 01:07 JST
- 「明日の予定を確認してください」画面の時刻表示エリアを調整し、出発時刻の右側に自動連係持ち物を表示するよう変更。
- 表示する持ち物は自動連係分のみ（宿題提出物・定期予定由来）とし、タグ情報付きで確認できるよう対応。
- 表示バージョンを `0.09` から `0.10` へ更新（+0.01）。

## 2026-07-27 00:55 JST
- 出発前チェック・持ち物連動仕様を追加し、出発前チェック画面に「今日の予定（出発/帰宅時刻）」と持ち物チェックリストを表示するよう変更。
- 持ち物リストは、締切が当日の宿題（提出物）、当日に該当する定期予定の持ち物、当日だけ追加した持ち物を統合し、重複名を自動で除外するよう実装。
- 定期予定の登録/編集画面に持ち物入力（追加・削除）を追加し、予定ごとの持ち物マスタを保持できるよう対応。
- 予定入力画面に「🎒 今日だけの持ち物」を追加し、対象日ごとに追加・削除できるよう対応。
- 表示バージョンを `0.08` から `0.09` へ更新（+0.01）。

## 2026-07-27 00:48 JST
- 出発前チェックの表示仕様を更新し、出発60分前〜15分前はホーム上部に「🟡 出発前チェック（あと○分）」通知と「今チェックする」ボタンを表示するよう変更。
- 出発15分前以降は出発前チェックを自動表示し、未完了の場合は3分ごとに再表示するよう変更。
- 出発前チェック画面に「ホームへ戻る」ボタンを追加し、戻った後も未完了なら再表示対象となるよう調整。
- 表示バージョンを `0.07` から `0.08` へ更新（+0.01）。

## 2026-07-27 00:41 JST
- 出発前チェックの「まだ」を、次へ進みつつ未確認項目が残るキュー方式に変更。
- 「確認した」と「まだ」の並びは維持し、未確認の項目は最後までチェック画面に残るようにした。

## 2026-07-27 00:39 JST
- 出発前チェック画面に「まだ」ボタンを追加し、「確認した」と横並びにした。
- 「まだ」を押した場合は未完了のまま残し、チェック状態を進めないようにした。

## 2026-07-27 00:27 JST
- 設定画面の最下部に、当日の予定・完了事項・出発前チェック・帰宅後チェックのステータスを初期化するボタンを追加。
- 実行中タスク、結果文、前日未完了表示もあわせて初期化し、当日の状態をクリーンに戻せるようにした。

## 2026-07-27 00:21 JST
- 表示バージョンを `0.06` から `0.07` へ更新（+0.01）。

## 2026-07-27 00:20 JST
- 帰宅後チェック画面の「チェック内容をコピー」と「帰宅後に上記を対応し共有した」ボタンを横並びに変更。

## 2026-07-27 00:19 JST
- 帰宅後チェック画面の「チェック内容をコピー」上に出ていた大きな吹き出しと説明文を削除し、表示を簡潔化。
- コピー機能は維持し、ボタン操作でチェック内容をコピーできるよう継続。

## 2026-07-27 00:17 JST
- 帰宅後チェック画面に、入力中のチェック内容をコピーできる欄とコピー按钮を追加。
- 帰宅後チェックの完了ボタン文言を「帰宅後に上記を対応し共有した」に変更。
- 表示バージョンは `0.06` のまま維持。

## 2026-07-27 00:00 JST
- 出発前チェックと帰宅後チェックの画面優先制御を修正し、ホーム画面にいても条件成立時は自動表示するよう変更。
- 出発前チェックは出発時刻到達後に一度だけ表示し、完了後はその日再表示しないよう維持。
- 帰宅後チェックも帰宅時刻到達後に一度だけ表示し、完了後はその日再表示しないよう維持。
- 表示バージョンを `0.05` から `0.06` へ更新（+0.01）。

## 2026-07-26 23:30 JST
- 開発用アラートテスト音量を 1〜30 から 1〜10 へ戻し、最大音量は従来より大きくなるようゲイン計算を強化。
- 音種プリセットを更新し、「時計のアラーム」「鳥の鳴き声」「クラクション」を追加（5種類構成を維持）。
- 表示バージョンを `0.04` から `0.05` へ更新（+0.01）。

## 2026-07-26 23:27 JST
- 開発用アラートテスト設定に「テスト鳴動秒数（1〜10秒）」を追加し、鳴る秒数を選択できるよう変更。
- テスト音量の最大値を従来の3倍に拡張（最小値は据え置き）し、設定レンジを 1〜30 に変更。
- テスト音の種類を5種類の名前付きプリセットへ更新し、電子音に加えて「ブザー音」「ベル風」「チャイム風」を選択可能に変更。
- 表示バージョンを `0.03` から `0.04` へ更新（+0.01）。

## 2026-07-26 23:25 JST
- 開発用「🔔 アラートテスト」に、テスト音量（1〜10）とテスト音の種類（1〜5）を設定できるUIを追加。
- テスト実行時のみ、選択した音量・音種を本番アラート関数 `triggerAlertFeedback` へ上書き指定して実行するよう変更（バイブは本番と同じ処理を継続）。
- 表示バージョンを `0.02` から `0.03` へ更新（+0.01）。

## 2026-07-26 23:23 JST
- 「🔔 アラートテスト」ボタンの動作を修正し、画面遷移・タスク開始・タイマー開始・タスク状態変更を行わず、本番アラート開始関数 `triggerAlertFeedback` のみ直接呼び出すよう変更。
- 設定画面上で本番と同じ警告表示（予定時間を超えました）を出すようにし、テスト実行をその場で確認可能に改善。
- 表示バージョンを `0.01` から `0.02` へ更新（+0.01）。

## 2026-07-26 23:14 JST
- ホーム画面ヘッダーの「Self Support PJR」表記に version を追加し、「Self Support PJR version 0.01」へ変更。
- 今後の更新では version を 0.01 ずつ増やす運用ルールを適用開始。

## 2026-07-26 23:13 JST
- 設定画面の最下部に開発用「🔔 アラートテスト」ボタンを追加。
- ボタン押下でタイマー待ちをせず、本番と同じアラート経路（checkOverrunNotification → triggerAlertFeedback）を直接実行するよう実装。
- 実行時は本番同様の超過表示を出すため、必要に応じて実行中タスクを開始/再開してから同一画面表示で発火するよう調整。

## 2026-07-26 22:59 JST
- 重大不具合対応として、入力中の全体再描画を抑制する入力ガード（フォーカス/IME合成監視）を追加。
- FirestoreのonSnapshot受信時、入力中は状態反映と再描画を保留し、入力終了後に適用するよう変更。
- 同期状態更新（saving/offline/online）に伴う不要な即時renderを削減し、受動的再描画を必要時のみ実行する方式へ変更。
- リモート同期反映時は現在画面phaseを維持するようにし、同期受信だけで勝手に画面遷移しにくいよう調整。

## 2026-07-26 22:50 JST
- 2026-07-26 21:51 JST 時点まで再ロールバック。
- 21:51以降に追加していたアラート関連修正と再読み込みボタン追加を取り下げ、21:51時点相当の状態へ復元。

## 2026-07-26 21:51 JST
- アプリ上部タイトル文言を全端末・全ユーザー共通で更新し、上段を「Self Support PJR」、主タイトルを「今日の予定を立てる」に変更。
- レイアウト・フォントサイズ・配色は変更せず、表示文言のみ差し替え。

## 2026-07-26 21:47 JST
- ホーム画面ヘッダー文言を変更し、上段を「SELF SUPPORT」、主タイトルを「今日の予定を立てる」に置き換え。
- 既存の位置・サイズ・配色・レイアウトは維持し、表示文言のみ更新。

## 2026-07-26 21:03 JST
- ホーム画面上部ナビの「ホーム」タグを「ログアウト」表示に変更。
- ホーム画面で同ボタン押下時にログアウト処理（Firebase signOut）が実行されるよう変更。
- 設定画面のログアウト処理と共通化し、失敗時のエラーメッセージ挙動を統一。

## 2026-07-26 20:39 JST
- 開発時の同期エラー調査用として、Firestore例外の詳細ログ（エラー内容・コード・失敗処理種別）をコンソール出力する診断機能を追加。
- 例外コードから原因候補を分類し、Firebase Console設定不足かFirestoreルール問題かを判別してログに表示するよう改善。
- 初回読み込み・初期登録・リアルタイム監視・保存処理ごとに失敗箇所を識別して出力するよう変更。
- ルール拒否/設定不足などの非回復系エラー時は再ログインまでFirestore再書き込みを停止し、同一原因の連続失敗を抑止。

## 2026-07-26 20:17 JST
- Cloud Firestore SDKを追加し、Authentication維持のまま users/{uid}/appState/main へアプリ状態を同期する基盤を実装。
- localStorage即時保存を維持しつつ、ログイン済み時のみFirestoreへ700msデバウンス保存する2段保存に対応。
- ログイン直後の初回同期を追加し、Firestore優先復元・Firestore空時のlocalStorage初期登録・両方空時の初期状態登録を実装。
- onSnapshotのリアルタイム監視を追加し、別端末更新の反映、受信時の検証・localStorage保存・再描画を実装。
- 無限保存ループ防止（ハッシュ比較・skipRemote保存・同一状態書き込み抑止）と、ログアウト時の監視解除/タイマー解除を実装。
- ホーム画面に小さい同期状態表示（同期中/保存中/同期済み/オフライン/同期エラー）を追加。

## 2026-07-26 19:57 JST
- Firebase Authentication（Email/Password）を導入し、ログイン状態監視（onAuthStateChanged）で画面表示を切り替えるよう変更。
- 未ログイン時はログイン画面（メールアドレス・パスワード・ログインボタン）のみ表示、ログイン済み時のみ既存アプリ画面を表示する制御を追加。
- 設定画面にログアウトボタンを追加し、現在ログイン中のメールアドレス表示とログアウト処理を実装。
- Firebase Authenticationの永続ログインにより、次回起動時の自動ログインに対応（Firestore同期は未実装）。

## 2026-07-26 19:52 JST
- Firebase Web SDK（modular）初期化コードを追加し、`selfsupportapp-web` プロジェクト設定を登録。
- `app.js` をES Modulesとして読み込むため、`index.html` のスクリプトタグを `type="module"` に変更。

## 2026-07-26 22:40 JST
- 出発前チェック文言を「提出物・持ち物を指差し確認・目視したか」に変更。
- 出発前チェック画面の「ホームへ進む」ボタンを削除。
- 出発前チェック中に他画面へ戻っても、画面遷移時は出発前チェックが優先表示される挙動を維持。

## 2026-07-26 12:54 JST
- 上部ナビの「戻る」ボタンを削除。
- 出発前チェック・帰宅後チェックが未完了でも、「ホーム」ボタン押下時はホーム画面へ戻れるように変更。

## 2026-07-26 14:14 JST
- ホーム画面を簡潔表示に変更し、時刻表示と1行タスク一覧（状態アイコン・タスク名・予定時間）へ再構成。
- ホーム画面のタスク行全体を押下可能にし、既存の実行画面遷移（中断中タスクは再開）に接続。
- ホーム画面に予定コピーボタンを追加し、既存の予定コピー文面生成ロジックを利用するように変更。
- タスク実行画面の選択カードに「内容」を表示し、遷移後に詳細確認できるよう改善。

## 2026-07-26 14:17 JST
- ホーム画面の状態表示を調整し、完了は「【完了】」、中断中は「【再開】」、未着手は「○」を表示するよう変更。
- 状態表示が崩れないようホームのステータス列幅を調整。

## 2026-07-26 14:21 JST
- ホーム画面のタスク一覧を同一テイストで統一し、各行に「状態・タスク名・予定時間・内容」を表示するよう変更。
- タスク行のレイアウトを2行表示に調整し、上段に状態/名称/分数、下段に内容を表示。

## 2026-07-26 14:24 JST
- ホーム画面の各タスクに「見積もり時間」と「実績時間」を表示するよう変更。
- 実績時間は実行中タスクで経過時間を表示し、未記録時は「-」を表示。
- 確認用として既存タスクにサンプル内容を投入し、ホーム上で内容が見える状態に更新。

## 2026-07-26 14:37 JST
- ホーム画面の下段ボタン順を調整し、「予定入力へ」を「予定をコピー」より前に表示するよう変更。

## 2026-07-26 14:42 JST
- 予定入力画面の時刻モード表記を「時刻を設定」から「時刻設定」に変更。

## 2026-07-26 14:47 JST
- 「今日の結果」画面をコンパクト化し、完了数/未完了数を横並び表示に変更。
- 合計時間表示を「予定 ○時間○分」「実績 ○時間○分」の横並びに変更。
- 各タスクを「項目：タスク名（完了/未完了）」「タスク名 予定 ○時間○分 実績 ○時間○分」「内容」の構成で表示。
- 報告文エリアの高さを縮小し、縦に長くなりすぎないよう調整。

## 2026-07-26 14:48 JST
- 結果画面上段の見出し「今日の結果」を削除。
- 「保護者への報告文」ブロックを画面上部に移動し、コピー/終了操作を先に実行できる配置へ変更。

## 2026-07-26 14:49 JST
- 結果画面の「各タスク」見出しを「保護者への報告事項（各タスク）」へ変更。
- 各タスクの時間表示を「○時間○分」形式から「○分」形式へ変更。

## 2026-07-26 14:52 JST
- 保護者への報告文タイトルを「今日の結果」から「○月○日結果」形式へ変更。
- 保護者への報告文内の予定時間/実績時間を「○分」表記から「○時間○分」表記へ変更。

## 2026-07-26 14:55 JST
- 保護者への報告文の未完了件数行を拡張し、「未完了：○件」に未完了タスク名を追記するよう変更。

## 2026-07-26 14:57 JST
- 保護者への報告文内の各項目行を「項目①」「項目②」形式の連番表記に変更。

## 2026-07-26 15:00 JST
- 保護者への報告文を2つ前の形式へ戻し、各タスクの内容を2行構成（1行目: 番号/項目/予定/実績、2行目: 内容）に復帰。

## 2026-07-26 15:06 JST
- 保護者への報告文で、各タスク名行と内容行の開始位置が揃うよう全角スペースの配置を調整。

## 2026-07-26 15:08 JST
- 結果画面の報告文フォントを元の設定に戻し、等幅フォント指定を削除。
- 保護者への報告文の開始位置ズレを修正し、番号行と内容行をタブ基準で揃える形式に変更。

## 2026-07-26 15:12 JST
- 保護者への報告文の内容折り返し位置調整を取り消し、ひとつ前のタブ基準レイアウトへ復帰。

## 2026-07-26 15:15 JST
- 保護者への報告文の本文改行ロジックを調整し、末尾だけ極端に短い行が出にくいよう本文の折り返しを均等化。

## 2026-07-26 15:22 JST
- 内容欄の表示を「ラベル」と「本文」の別要素に分離（task-content-row / task-content-label / task-content-text）。
- 内容本文の折り返し開始位置が常に「内容：」直後に揃うよう修正。
- 内容欄を表示する各画面（確認、実行、レビュー、結果一覧、報告文表示）へ同じ構造を適用。

## 2026-07-26 15:25 JST
- 1日の終了画面の報告欄にも同じ内容行構造（task-content-row）を適用し、結果画面と同じ折り返し位置で表示するよう統一。

## 2026-07-26 15:30 JST
- 保護者への報告文表示にタスク単位のブロック余白を追加し、実績時間と各項目の間・①と②の間の改行が視覚的に分かるよう改善。

## 2026-07-26 15:35 JST
- 保護者への報告文で各内容行の開始位置を調整し、項目名開始位置（⑤の日記の「日」位置）と揃うようオフセットを追加。

## 2026-07-26 15:29 JST
- 予定の最終確認画面に「この画面をコピー」ボタンを追加。
- 1日の終了時に当日の実績報告文を保持し、次の日の予定報告画面で「実績+次の日予定をメール作成」できるフローを追加。

## 2026-07-26 15:33 JST
- 予定コピー文の形式を更新し、「○月○日（曜日）の予定」見出し、合計時間の時間分表記、番号付きタスク＋内容行の構成へ変更。

## 2026-07-26 15:43 JST
- 予定入力画面の「予定時間」を、選択式（その他）から数値の直接入力方式（○分）へ変更。
- 予定時間入力欄に分数候補（10/20/30/40/60分）のサジェストを追加しつつ、任意の分数をそのまま登録できるよう改善。

## 2026-07-26 15:49 JST
- 予定時間の分数プリセット（10/20/30/40/60）を横並び表示に変更。
- 各プリセットボタンの横幅を約1cmに統一し、5つが並んで選べる見た目へ調整。

## 2026-07-26 15:55 JST
- 予定入力画面で、完了表示タスクでも「修正」「削除」「並び替え」が使えるように変更。
- 明日の予定として確定する際は、各タスクの実行状態を pending にリセットし、翌日実行で未完了扱いから開始できるよう修正。

## 2026-07-26 16:00 JST
- ホーム画面下部の「予定をコピー」ボタンを削除。
- ホーム画面の下段操作を「予定入力へ」「1日の終了」の2ボタン構成に変更。

## 2026-07-26 16:16 JST
- Android Chrome向けに、通知音用 AudioContext をアプリ内で再利用する実装へ変更し、タスク開始/再開のユーザー操作時に初期化・resume を実行するよう調整。
- 予定時間超過通知で音とバイブを個別関数化し、vibrate戻り値のログ出力・失敗時の console.error 出力を追加。
- タスク実行画面に確認用「音をテスト」「バイブをテスト」ボタンを追加。
- 超過通知判定時のデバッグログと、実行中タスクを1分テスト設定にするボタンを追加。

## 2026-07-26 16:34 JST
- ホーム画面右上に「⚙️設定」ボタンを追加し、設定画面から「定期予定」メニューへ遷移できるように変更。
- 定期予定（予定名・予定時間・内容・曜日複数選択）の登録/修正/削除機能を追加。
- 予定入力画面を開いた際、対象日（今日/明日）に一致する定期予定をその日分として自動追加する処理を追加。
- 自動追加後の予定は通常予定と同じく削除・時間変更・内容変更・並び替えが可能で、当日編集が定期予定マスタへ影響しないよう分離。

## 2026-07-26 16:44 JST
- 予定入力（予定登録）フォームに「繰り返し（毎日/曜日指定/なし）」「締切（なし/日付指定）」「Googleカレンダー同期（ON/OFF）」を追加。
- 曜日指定選択時のみ曜日チェックを表示し、日付指定選択時のみ締切日入力を表示する条件表示に対応。
- 入力した3項目を各予定データへ保存・復元できるようタスク正規化と編集読み込み処理を拡張。

## 2026-07-26 16:57 JST
- 予定データを「今日の予定」「定期予定」「宿題・課題」の3種類に画面と保存先で分離し、通常予定フォームから繰り返し/締切/Google同期項目を削除。
- 設定画面を「定期予定 ＞」1行構成に維持したまま、定期予定の登録項目を「予定名・予定時間・内容・繰り返し（毎日/曜日指定）・Google同期」に整理。
- ホーム画面に「宿題・課題」ボタン（未完了件数表示付き）を追加し、宿題・課題一覧/編集画面（課題名・締切・内容・Google同期・完了状態）を新設。
- 宿題・課題一覧は未完了のみを締切日昇順で1行表示し、完了済みは未完了一覧から除外。
- 定期予定の自動追加処理を通常予定作成時に維持し、当日側の削除/時間変更/内容変更/並び替えが定期予定マスタへ影響しない構成を維持。

## 2026-07-26 17:20 JST
- タスク実行画面から「音をテスト」「バイブをテスト」ボタンを削除。
- テストボタンに紐づくイベント登録処理を削除し、通常の実行/中断/完了フローには影響しないよう整理。